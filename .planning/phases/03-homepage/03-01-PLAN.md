---
phase: 03-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/homepage.ts
  - src/data/testimonials.ts
  - src/data/events.ts
  - src/data/partners.ts
  - src/data/homepage.ts
  - src/components/home/Hero.tsx
  - src/components/home/SelfIdCards.tsx
  - src/components/home/ServicePillars.tsx
autonomous: true

must_haves:
  truths:
    - "Hero displays mission statement with landscape imagery and dual CTAs"
    - "Self-identification cards show 4 audience types that link to /who-we-serve#[segment]"
    - "Four service pillars render as interactive cards in a 2x2 grid with hover effects"
  artifacts:
    - path: "src/types/homepage.ts"
      provides: "TypeScript types for all homepage data structures"
      exports: ["Testimonial", "Event", "Partner", "ServicePillar", "SelfIdCard", "ImpactStat"]
    - path: "src/data/testimonials.ts"
      provides: "Sample testimonial data for 4 audience segments"
      exports: ["testimonials"]
    - path: "src/data/events.ts"
      provides: "Sample event data with dates, types, formats"
      exports: ["events"]
    - path: "src/data/partners.ts"
      provides: "Sample partner organization data"
      exports: ["partners"]
    - path: "src/data/homepage.ts"
      provides: "Homepage-specific content data (pillars, self-id cards, impact stats)"
      exports: ["servicePillars", "selfIdCards", "impactStats"]
    - path: "src/components/home/Hero.tsx"
      provides: "Hero section with mission statement, landscape bg, dual CTAs, entrance animation"
      min_lines: 60
    - path: "src/components/home/SelfIdCards.tsx"
      provides: "Four audience self-identification cards with links"
      min_lines: 40
    - path: "src/components/home/ServicePillars.tsx"
      provides: "2x2 grid of service pillar cards with hover effects"
      min_lines: 50
  key_links:
    - from: "src/components/home/SelfIdCards.tsx"
      to: "/who-we-serve#farmers, #offshore, #families, #community"
      via: "Next.js Link href"
      pattern: "Link.*who-we-serve"
    - from: "src/components/home/ServicePillars.tsx"
      to: "/services/[slug]"
      via: "Next.js Link href"
      pattern: "Link.*services/"
    - from: "src/data/homepage.ts"
      to: "src/types/homepage.ts"
      via: "TypeScript imports"
      pattern: "import.*from.*types/homepage"
---

<objective>
Create the homepage data layer (types + sample data for all homepage sections) and the top-of-page components: Hero, Self-Identification Cards, and Service Pillars.

Purpose: Establishes the typed data foundation that all homepage components consume, plus builds the most critical above-the-fold content that visitors see first -- the mission statement, audience routing, and service overview.

Output: 8 files -- 1 type definition, 4 data files, 3 React components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create homepage types and data files</name>
  <files>
    src/types/homepage.ts
    src/data/testimonials.ts
    src/data/events.ts
    src/data/partners.ts
    src/data/homepage.ts
  </files>
  <action>
    Create TypeScript types and sample data for the entire homepage. This data layer is shared across all homepage component plans.

    **src/types/homepage.ts** -- Export these interfaces:

    ```typescript
    export interface Testimonial {
      id: string;
      quote: string;
      name: string;
      identifier: string; // e.g. "dairy farmer near Huntly"
      audience: 'farmer' | 'offshore' | 'family' | 'community';
    }

    export interface Event {
      id: string;
      title: string;
      date: string; // ISO date string
      time: string;
      location: string;
      format: 'in-person' | 'virtual' | 'hybrid';
      type: 'workshop' | 'social' | 'volunteer' | 'support';
      audience: ('farmer' | 'offshore' | 'family' | 'community')[];
      description: string;
      slug: string;
    }

    export interface Partner {
      id: string;
      name: string;
      logoUrl?: string; // Optional -- use placeholder if not provided
    }

    export interface ServicePillar {
      id: string;
      title: string; // User-facing plain language name
      description: string;
      icon: string; // Lucide icon name
      slug: string; // URL slug for /services/[slug]
      color: string; // Tailwind color class for accent
    }

    export interface SelfIdCard {
      id: string;
      label: string; // "Farmer", "Offshore Worker", etc.
      description: string; // Brief one-liner
      href: string; // Link destination
      icon: string; // Lucide icon name
    }

    export interface ImpactStat {
      id: string;
      value: number;
      suffix?: string; // e.g. "+" or "%"
      label: string;
    }
    ```

    **src/data/testimonials.ts** -- Export array of 4 testimonials (one per audience), using exact quotes from CLAUDE.md:
    - James, dairy farmer near Huntly (farmer)
    - An offshore worker testimonial (offshore)
    - A community member testimonial (community)
    - A family member testimonial (family)

    **src/data/events.ts** -- Export array of 3-4 sample events with realistic future dates (use dates 2-4 weeks from now), varied formats and types. Include events like:
    - "Farm Wellbeing Workshop" (in-person, workshop, farmer audience)
    - "Offshore Workers Peer Support" (virtual, support, offshore audience)
    - "Community Coffee Morning" (in-person, social, all audiences)

    **src/data/partners.ts** -- Export array of 5-6 placeholder partner organizations with names like:
    - "RSABI"
    - "NHS Grampian"
    - "Aberdeenshire Council"
    - "Scottish Rural Network"
    - "Samaritans Scotland"
    (No actual logo URLs -- use undefined; the component will handle placeholder display)

    **src/data/homepage.ts** -- Export these arrays:

    `servicePillars` -- 4 items using the user-facing names from RESEARCH.md:
    1. "Supporting Your Wellbeing" (slug: "community-wellness", icon: "Heart")
    2. "Supporting Farm Life" (slug: "agricultural-support", icon: "Wheat")
    3. "Supporting Those Who Work Away" (slug: "offshore-support", icon: "Anchor")
    4. "Strengthening Our Community" (slug: "community-connection", icon: "Users")

    Each with a 1-2 sentence description in the warm, neighbour-like tone.

    `selfIdCards` -- 4 items:
    1. "Farmer" -> /who-we-serve#farmers (icon: "Tractor")
    2. "Offshore Worker" -> /who-we-serve#offshore (icon: "Ship")
    3. "Family Member" -> /who-we-serve#families (icon: "Home")
    4. "Community Member" -> /who-we-serve#community (icon: "HandHeart")

    Each with a brief, warm description like "Find support tailored to life on the farm."

    `impactStats` -- 4 items with realistic placeholder numbers:
    1. 500+ people supported
    2. 50+ events hosted
    3. 20+ community partners
    4. 4 service areas

    All data files must import types from `@/types/homepage` and satisfy the type contracts.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- all type files compile without errors.
    Verify each data file exports the expected named export.
  </verify>
  <done>
    All 5 files exist. Types are exported and used consistently. Data files contain realistic sample content matching the tone described in RESEARCH.md. No `any` types used.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Hero section component</name>
  <files>src/components/home/Hero.tsx</files>
  <action>
    Create the Hero section -- the first thing visitors see. This is the most important component on the homepage.

    **Layout:**
    - Full-width section with a subtle landscape background (use a CSS gradient simulating a Scottish landscape for now -- deep navy to stone, or use a placeholder approach with `next/image` and a data URI or local placeholder)
    - Semi-transparent dark overlay on the background for text readability
    - Content centered, max-width container (matching site layout)
    - Generous vertical padding: py-24 on mobile, py-32 on desktop

    **Content (top to bottom):**
    1. Gold accent line (thin horizontal, 80px wide, centered) -- this draws in first
    2. Overline text: "ST KATHARINE RURAL CONNECT" in uppercase, DM Sans 700, wide tracking, text-accent-500
    3. Main heading (h1): "Strengthening rural communities through support, connection, and care" -- font-heading (Cormorant Garamond), text-display on desktop / text-5xl on mobile, text-white
    4. Subheading: "We serve farmers, offshore workers, families, and community members by creating accessible pathways to wellness, practical resources, and meaningful belonging." -- font-body, text-lg, text-stone-200, max-w-2xl mx-auto
    5. Two CTA buttons side by side (flex row, gap-4, centered):
       - Primary: "Get in Touch" -> /contact (solid accent-500 bg, text-primary-950, hover:bg-accent-400, rounded, px-8 py-3, font-medium)
       - Secondary: "See What's On" -> /events (border-2 border-white text-white hover:bg-white/10, rounded, px-8 py-3, font-medium)
    6. Crisis line text below CTAs: "Need to talk now? Call [phone number]" with the phone number as a `tel:` link, text-stone-300, text-sm

    **Entrance animation (using Motion):**
    - Use `"use client"` directive since this uses Motion
    - Import `motion` from `motion/react` (NOT framer-motion -- the package is now called "motion")
    - Wrap in a `motion.section`
    - Gold accent line: draws width from 0 to 80px over 800ms
    - Overline: fade up (y: 20 -> 0, opacity 0 -> 1), 600ms, delay 200ms
    - Heading: fade up, 600ms, delay 400ms
    - Subheading: fade up, 600ms, delay 550ms
    - CTAs: fade up, 600ms, delay 700ms
    - Crisis text: fade up, 600ms, delay 850ms
    - Use `cubic-bezier(0.25, 0.1, 0.25, 1)` easing for all
    - Respect `prefers-reduced-motion`: use `useReducedMotion()` from motion/react. When true, set all initial states to final states (no animation).

    **Responsive:**
    - Mobile: stack CTAs vertically (flex-col), smaller heading
    - Desktop: CTAs side by side (flex-row)
    - Text always centered

    **Background approach:** Use a CSS gradient as placeholder: `bg-gradient-to-br from-primary-950 via-primary-900 to-primary-800`. This will be replaced with actual landscape imagery later. The gradient should have a subtle overlaid pattern or texture feel. Add a comment `{/* TODO: Replace gradient with actual landscape image using next/image */}`.

    **Accessibility:**
    - Section has `aria-label="Welcome to St Katharine Rural Connect"`
    - h1 is the only h1 on the page
    - CTAs use Next.js `Link` component
    - Phone number uses `<a href="tel:+441234567890">` (placeholder number)
  </action>
  <verify>
    Component renders without errors in `npm run dev`.
    Inspect: h1 present, two CTA links present, tel: link present.
    Animation plays on load (check with browser dev tools or visual inspection).
    Resize to mobile -- CTAs stack vertically, heading shrinks.
  </verify>
  <done>
    Hero.tsx exists and exports a default component. It displays the mission statement heading, subheading, two CTA buttons, and a crisis phone link. Entrance animation plays with staggered timing. Responsive layout works at mobile and desktop breakpoints. Reduced motion preference is respected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Self-ID Cards and Service Pillars components</name>
  <files>
    src/components/home/SelfIdCards.tsx
    src/components/home/ServicePillars.tsx
  </files>
  <action>
    **SelfIdCards.tsx -- "I am a..." audience routing section (HOME-02):**

    - `"use client"` for Motion animations
    - Import `selfIdCards` from `@/data/homepage`
    - Import Lucide icons dynamically or use a mapping object (prefer mapping: `{ Tractor, Ship, Home, HandHeart }` from `lucide-react`)

    Layout:
    - SectionWrapper-style container: max-w-6xl mx-auto, px-4, py-16 md:py-24
    - Section background: stone-100 (passed via className or handled by parent)
    - Section heading: "Find Support For You" or "I am a..." in overline style above, then "How Can We Help?" as h2 (font-heading, text-4xl, text-center)
    - 4 cards in a responsive grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-4, gap-6

    Each card:
    - White background (bg-white), rounded-lg, p-6, shadow-sm
    - Hover: lift 4px (translateY(-4px)), shadow-md, gold 2px top-border appears (use `::before` pseudo-element or a motion div that scales from scaleX(0) to scaleX(1) on group-hover)
    - Icon at top: 40x40, text-accent-500
    - Label: font-heading, text-xl, font-semibold, mt-3
    - Description: text-body text-sm, text-text-muted, mt-2
    - Arrow or "Learn more" link indicator at bottom
    - Entire card is a Next.js `Link` wrapping the card content (the card itself is clickable)

    Staggered reveal animation:
    - Cards fade up with 120ms stagger using `motion.div` with `whileInView`, `once: true`, `viewport: { margin: "-100px" }`
    - Respect reduced motion

    **ServicePillars.tsx -- Four service pillar cards (HOME-03):**

    - `"use client"` for Motion animations
    - Import `servicePillars` from `@/data/homepage`
    - Import Lucide icons via mapping: `{ Heart, Wheat, Anchor, Users }` from `lucide-react`

    Layout:
    - SectionWrapper-style: max-w-6xl mx-auto, px-4, py-16 md:py-24
    - Section background: stone-50
    - Overline: "OUR SERVICES" (uppercase, tracking-widest, text-accent-600, text-sm, font-bold, mb-3, text-center)
    - Heading: "Four Pillars of Support" as h2, font-heading, text-4xl, text-center, mb-4
    - Brief intro paragraph centered, max-w-2xl mx-auto, mb-12
    - 2x2 grid: grid-cols-1 md:grid-cols-2, gap-6 md:gap-8

    Each pillar card:
    - bg-white rounded-lg p-8 shadow-sm, relative overflow-hidden
    - Gold top border effect on hover (same pattern as SelfIdCards)
    - Card lift on hover: translateY(-4px), shadow-md
    - Icon: 48x48, contained in a rounded-full bg-stone-100 p-3 wrapper, text-primary-700
    - Title: font-heading, text-2xl, font-semibold, mt-4
    - Description: font-body, text-base, text-text-body, mt-3, line-clamp-3
    - "Learn more" link text at bottom: text-accent-600, flex items-center gap-1, with a small arrow icon
    - Entire card wrapped in Next.js `Link` to `/services/${pillar.slug}`

    Staggered reveal:
    - Same whileInView fade-up with 120ms stagger
    - Respect reduced motion

    **Shared hover pattern (DRY):** Both components use the same card hover effect (4px lift + gold top border). Implement this as Tailwind utility classes that can be composed:
    - `group` on the card wrapper
    - `transition-all duration-300 ease-premium` for the card
    - `hover:-translate-y-1 hover:shadow-md` for the lift
    - Gold border: an absolutely-positioned div at top, `h-0.5 bg-accent-500 transition-transform duration-300 origin-left scale-x-0 group-hover:scale-x-100`

    If the shared layout components from Phase 2 (like SectionWrapper) are available, use them. If not, inline the section wrapper pattern (container, padding, background) and add a comment: `{/* TODO: Extract to SectionWrapper when Phase 2 is complete */}`.
  </action>
  <verify>
    Both components render without errors.
    SelfIdCards: 4 cards visible, each links to /who-we-serve#[segment], hover effect works.
    ServicePillars: 4 cards in 2x2 grid on desktop, 1 column on mobile, each links to /services/[slug].
    Check hover: cards lift, gold border appears from left.
  </verify>
  <done>
    SelfIdCards.tsx renders 4 audience cards in a responsive grid. Each card links to the correct /who-we-serve#[anchor] URL. Hover effects (lift + gold border) work. ServicePillars.tsx renders 4 pillar cards in a 2x2 grid. Each links to /services/[slug]. Both components have staggered whileInView reveal animations and respect prefers-reduced-motion.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 8 files exist at their specified paths
- Data files export typed arrays (no `any`)
- Components import data from `@/data/` and types from `@/types/`
- All interactive elements use Next.js `Link` (not `<a>` tags for internal routes)
- Hover effects use the premium card pattern (4px lift, gold top border from left)
- Animations use `motion/react` (not `framer-motion`)
- `useReducedMotion()` is checked in animated components
</verification>

<success_criteria>
- HOME-01 (Hero): Mission statement heading, dual CTAs, crisis phone link, entrance animation
- HOME-02 (Self-ID): 4 audience cards routing to /who-we-serve#[segment]
- HOME-03 (Pillars): 4 service cards in 2x2 grid with hover effects, linking to /services/[slug]
- All components typed, animated, responsive, and accessible
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-01-SUMMARY.md`
</output>
