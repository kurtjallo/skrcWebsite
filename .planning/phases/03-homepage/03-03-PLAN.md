---
phase: 03-homepage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/GetInvolved.tsx
  - src/components/home/Newsletter.tsx
  - src/components/home/PartnerLogos.tsx
autonomous: true

must_haves:
  truths:
    - "Get Involved section presents Volunteer, Partner, and Donate options with clear CTAs"
    - "Newsletter signup form captures an email address with a submit button"
    - "Partner logos strip displays organization names/placeholders in a grayscale row"
  artifacts:
    - path: "src/components/home/GetInvolved.tsx"
      provides: "Three engagement pathways with CTAs"
      min_lines: 50
    - path: "src/components/home/Newsletter.tsx"
      provides: "Email signup form section"
      min_lines: 40
    - path: "src/components/home/PartnerLogos.tsx"
      provides: "Partner organizations display strip"
      min_lines: 30
  key_links:
    - from: "src/components/home/GetInvolved.tsx"
      to: "/get-involved, /partnerships, /contact"
      via: "Next.js Link href on CTA buttons"
      pattern: "Link.*href.*(get-involved|partnerships|contact)"
    - from: "src/components/home/PartnerLogos.tsx"
      to: "src/data/partners.ts"
      via: "imports partners array"
      pattern: "import.*partners.*from.*data/partners"
---

<objective>
Build the bottom-of-page homepage sections: Get Involved engagement pathways, Newsletter signup form, and Partner Logos strip.

Purpose: These sections convert passive visitors into engaged community members -- volunteering, donating, subscribing, and seeing organizational credibility through partnerships.

Output: 3 React components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@RESEARCH.md
@src/types/homepage.ts
@src/data/partners.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build GetInvolved component</name>
  <files>src/components/home/GetInvolved.tsx</files>
  <action>
    Create the Get Involved section with three engagement pathways (HOME-07).

    - `"use client"` for Motion animations
    - Import `Link` from `next/link`
    - Import icons from `lucide-react`: `HandHelping`, `Handshake`, `Heart` (or similar)

    **Background:** stone-100 (alternating rhythm).

    **Layout:**
    - Section: py-16 md:py-24, bg-stone-100
    - Content: max-w-6xl mx-auto, px-4
    - Overline: "GET INVOLVED" (uppercase, tracking-widest, text-accent-600, text-sm, font-bold, text-center, mb-3)
    - Heading: "Be Part of Something Meaningful" as h2, font-heading, text-4xl, text-center, mb-4
    - Brief intro: "There are many ways to support rural communities -- and every contribution makes a difference." -- text-center, text-text-muted, max-w-2xl mx-auto, mb-12

    **Three cards in a responsive grid:** grid-cols-1 md:grid-cols-3, gap-6 md:gap-8

    Card 1 -- Volunteer:
    - Icon: HandHelping, 48x48, text-primary-600, in a rounded bg-white p-3 wrapper
    - Title: "Volunteer With Us" (font-heading, text-2xl, font-semibold, mt-4)
    - Description: "Give your time and skills to help strengthen your community. From events to mentoring, there's a role for everyone." (font-body, text-base, text-text-body, mt-3)
    - CTA button: "Find Out How" -> /get-involved (styled as secondary: border-2 border-primary-600 text-primary-600 hover:bg-primary-600 hover:text-white, rounded, px-6 py-2.5, mt-6, font-medium, transition-colors duration-300)

    Card 2 -- Partner:
    - Icon: Handshake, same styling
    - Title: "Become a Partner"
    - Description: "Join a network of organisations committed to rural wellbeing. Together, we can reach more people and make a bigger impact."
    - CTA: "Partner With Us" -> /partnerships

    Card 3 -- Donate:
    - Icon: Heart, same styling
    - Title: "Support Our Work"
    - Description: "Your donation helps us keep services free and accessible for everyone who needs them. Every contribution counts."
    - CTA: "Donate Today" -> /contact (placeholder -- will link to donation page when available)
    - Note: The Donate CTA should use the primary style: bg-accent-500 text-primary-950 hover:bg-accent-400 (distinguishing it from the other two)

    **Each card:**
    - bg-white rounded-lg p-8, text-center
    - Hover: same lift pattern (translateY(-4px), shadow-md, gold top border from left)
    - Cards are NOT links themselves (they contain a CTA button inside)

    **Animation:**
    - Staggered fade-up, 120ms delay, whileInView, once
    - Respect reduced motion

    **Accessibility:**
    - Section `aria-label="Get involved"`
    - Each card uses proper heading hierarchy (h3 under the section h2)
  </action>
  <verify>
    Component renders 3 cards. Volunteer links to /get-involved, Partner to /partnerships, Donate to /contact.
    Donate button has distinct gold styling. Cards have hover lift effect.
    Mobile: single column. Desktop: 3 columns.
  </verify>
  <done>
    GetInvolved.tsx renders 3 engagement cards (Volunteer, Partner, Donate) in a responsive grid. Each has an icon, title, description, and CTA button linking to the correct page. Donate button has distinct primary/gold styling. Cards animate on scroll.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Newsletter and PartnerLogos components</name>
  <files>
    src/components/home/Newsletter.tsx
    src/components/home/PartnerLogos.tsx
  </files>
  <action>
    **Newsletter.tsx -- Email signup form (HOME-08):**

    - `"use client"` for form state + Motion animations
    - Import `useState` from `react`

    **Background:** stone-50 (alternating rhythm).

    **Layout:**
    - Section: py-16 md:py-24, bg-stone-50
    - Content: max-w-2xl mx-auto, px-4, text-center

    **Content:**
    - Heading: "Stay in the Loop" as h2, font-heading, text-4xl, text-center, mb-4
    - Description: "Get updates on events, workshops, and community news delivered to your inbox. No spam, just the good stuff." -- text-text-muted, text-center, mb-8

    **Form:**
    - Single row on desktop: flex row with email input + submit button
    - Stack on mobile: flex-col
    - Email input:
      - `type="email"`, `required`, `placeholder="Your email address"`, `autocomplete="email"`
      - Styling: bg-white border border-stone-300 rounded-l-lg (desktop) or rounded-lg (mobile), px-4 py-3, text-base, w-full, focus:outline-none focus:ring-2 focus:ring-accent-500 focus:border-accent-500
      - Visible `<label>` for accessibility: use `sr-only` class on the label text "Email address" so it's screen-reader accessible but visually hidden (the placeholder serves as the visual cue)
    - Submit button:
      - Text: "Subscribe"
      - Styling: bg-accent-500 text-primary-950 font-medium px-8 py-3, rounded-r-lg (desktop) or rounded-lg mt-3 w-full (mobile), hover:bg-accent-400, transition-colors duration-300
      - On hover: the gold background sweep animation (or simple color transition)

    **Form state:**
    - `const [email, setEmail] = useState('')`
    - `const [status, setStatus] = useState<'idle' | 'success' | 'error'>('idle')`
    - On submit: prevent default, validate email is non-empty and contains @, set status to 'success', clear email. Display "Thanks for subscribing!" success message in text-success.
    - No actual API call for MVP. Add comment: `// TODO: Connect to email service (Mailchimp, ConvertKit, etc.)`
    - On error (invalid email): set status to 'error', display "Please enter a valid email address" in text-error.

    **Honeypot field (spam prevention):**
    - Add a hidden text input: `<input type="text" name="website" className="hidden" tabIndex={-1} autoComplete="off" aria-hidden="true" />`
    - If this field has a value on submit, silently ignore the submission (bot filled it in)

    **Animation:**
    - Section fades up, whileInView, once
    - Respect reduced motion

    **Accessibility:**
    - Form has `aria-label="Newsletter signup"`
    - Live region for status messages: `aria-live="polite"` on the status message container
    - Label properly linked to input via htmlFor/id

    ---

    **PartnerLogos.tsx -- "Supported by" partner strip (HOME-09):**

    - `"use client"` for Motion animations (optional -- could be server component if no animation needed)
    - Import `partners` from `@/data/partners`

    **Background:** stone-100 (final alternating section before footer).

    **Layout:**
    - Section: py-12 md:py-16, bg-stone-100
    - Content: max-w-6xl mx-auto, px-4, text-center

    **Content:**
    - Overline/heading: "Supported By" or "Our Partners" -- text-text-muted, text-sm, uppercase, tracking-widest, font-bold, mb-8, text-center

    **Partner display:**
    - Flex row, flex-wrap, justify-center, items-center, gap-8 md:gap-12
    - Each partner:
      - If `logoUrl` exists: render with `next/image`, grayscale filter (filter grayscale), opacity-50, hover:grayscale-0 hover:opacity-100, transition-all duration-300, max-h-10 w-auto
      - If no `logoUrl` (our case for MVP): render partner name as text -- font-body, text-lg, text-text-muted, opacity-50, hover:opacity-100, transition-opacity duration-300. This provides a clean text-based partner display until real logos are available.
    - Add comment: `{/* TODO: Replace text names with actual partner logos */}`

    **Visual polish:**
    - A thin gold line above the section (1px, full width or max-w-4xl centered, bg-accent-500 opacity-20) to separate from newsletter section
    - Partners row should feel understated -- these are trust signals, not CTAs

    **Animation (subtle):**
    - Simple fade in, whileInView, once
    - No stagger needed (it's a single row of logos)
    - Respect reduced motion

    **Accessibility:**
    - Section `aria-label="Our partners and supporters"`
    - If using images: meaningful alt text with partner name
    - If using text: no special treatment needed
  </action>
  <verify>
    Newsletter: email input + subscribe button visible. Submit with valid email shows success message. Submit with empty/invalid email shows error. Honeypot field is hidden but present in DOM.
    PartnerLogos: all partner names display in a centered row. Hover increases opacity. Section has "Supported By" heading.
  </verify>
  <done>
    Newsletter.tsx renders an email signup form with input, submit button, honeypot field, success/error states, and proper accessibility labels. PartnerLogos.tsx renders partner names in a subtle, grayscale-styled row with hover reveal. Both components are responsive and animate on scroll.
  </done>
</task>

</tasks>

<verification>
- All 3 component files exist at specified paths
- `npx tsc --noEmit` passes
- Newsletter form has: email input with label, honeypot hidden field, submit button, success/error feedback
- GetInvolved has 3 distinct cards with correct CTA destinations
- PartnerLogos imports from `@/data/partners` (not hardcoded)
- All animations use `motion/react` and respect `useReducedMotion()`
- All form elements have proper labels and aria attributes
</verification>

<success_criteria>
- HOME-07 (Get Involved): 3 engagement cards with Volunteer/Partner/Donate CTAs
- HOME-08 (Newsletter): Email form with validation, honeypot, success/error states
- HOME-09 (Partner Logos): Partner names displayed in a subtle strip
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-03-SUMMARY.md`
</output>
