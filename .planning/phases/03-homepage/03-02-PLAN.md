---
phase: 03-homepage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/ImpactStats.tsx
  - src/components/home/FeaturedTestimonial.tsx
  - src/components/home/EventsPreview.tsx
autonomous: true

must_haves:
  truths:
    - "Impact statistics display with animated counters that count up when scrolled into view"
    - "Featured testimonial renders in a dark section with gold accent quote marks"
    - "Upcoming events preview shows 2-3 events with date, title, and a View All link"
  artifacts:
    - path: "src/components/home/ImpactStats.tsx"
      provides: "Animated counter statistics section"
      min_lines: 60
    - path: "src/components/home/FeaturedTestimonial.tsx"
      provides: "Dark section testimonial with gold accents"
      min_lines: 50
    - path: "src/components/home/EventsPreview.tsx"
      provides: "Upcoming events preview with View All link"
      min_lines: 60
  key_links:
    - from: "src/components/home/ImpactStats.tsx"
      to: "src/data/homepage.ts"
      via: "imports impactStats array"
      pattern: "import.*impactStats.*from.*data/homepage"
    - from: "src/components/home/FeaturedTestimonial.tsx"
      to: "src/data/testimonials.ts"
      via: "imports testimonials array"
      pattern: "import.*testimonials.*from.*data/testimonials"
    - from: "src/components/home/EventsPreview.tsx"
      to: "src/data/events.ts"
      via: "imports events array and sorts/filters by date"
      pattern: "import.*events.*from.*data/events"
---

<objective>
Build the mid-page homepage sections: Impact Statistics with animated counters, Featured Testimonial in a dark dramatic section, and Upcoming Events preview.

Purpose: These three sections provide social proof (stats, testimonial) and practical value (events) -- the persuasion layer that moves visitors from awareness to engagement.

Output: 3 React components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@RESEARCH.md
@src/types/homepage.ts
@src/data/homepage.ts
@src/data/testimonials.ts
@src/data/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ImpactStats component with animated counters</name>
  <files>src/components/home/ImpactStats.tsx</files>
  <action>
    Create the impact statistics section with animated number counters (HOME-04).

    - `"use client"` directive
    - Import `impactStats` from `@/data/homepage`
    - Import `motion`, `useInView`, `useMotionValue`, `useTransform`, `animate` from `motion/react`
    - Import `useEffect`, `useRef` from `react`

    **Background:** primary-900 (dark dramatic section) -- this is the first dark break in the page rhythm.

    **Layout:**
    - Full-width dark section: bg-primary-900, py-16 md:py-24
    - Content: max-w-6xl mx-auto, px-4
    - Optional overline: "OUR IMPACT" in uppercase, text-accent-500, tracking-widest, text-center, mb-8
    - Stats in a responsive grid: grid-cols-2 md:grid-cols-4, gap-8 md:gap-12
    - Each stat centered

    **Each stat item:**
    - Number: font-heading, text-5xl md:text-display, font-light (weight 300), text-white
    - Suffix ("+", "%") appended to the number, same style
    - Label below: font-body, text-base, text-stone-300, mt-2

    **Animated counter:**
    Create a reusable `AnimatedCounter` component (can be internal to the file):

    ```typescript
    function AnimatedCounter({ value, suffix = '', duration = 2 }: { value: number; suffix?: string; duration?: number }) {
      const ref = useRef<HTMLSpanElement>(null);
      const inView = useInView(ref, { once: true, margin: "-100px" });
      const motionValue = useMotionValue(0);
      const rounded = useTransform(motionValue, (v) => Math.round(v));

      useEffect(() => {
        if (inView) {
          animate(motionValue, value, { duration, ease: "easeOut" });
        }
      }, [inView, value, duration, motionValue]);

      useEffect(() => {
        const unsubscribe = rounded.on("change", (v) => {
          if (ref.current) {
            ref.current.textContent = `${v}${suffix}`;
          }
        });
        return unsubscribe;
      }, [rounded, suffix]);

      return <span ref={ref}>0{suffix}</span>;
    }
    ```

    The counter should:
    - Start at 0 when out of view
    - Count up to the target value over 2 seconds when scrolled into view
    - Only animate once (not re-trigger on scroll back)
    - Use easeOut easing for a natural deceleration feel

    **Reduced motion:** When `useReducedMotion()` returns true, skip animation and display final values immediately.

    **Accessibility:**
    - Each stat should have `role="figure"` with `aria-label` describing the stat (e.g., "500+ people supported")
    - The section has `aria-label="Our impact in numbers"`

    **Visual polish:**
    - Optional: thin gold accent line (1px) between the overline and the stats grid, centered, 60px wide
    - Stats should have a subtle divider between them on desktop (border-r border-primary-700 on all but last, or use a different visual separator)
  </action>
  <verify>
    Component renders 4 stat items on dark background.
    Scroll to the section -- numbers animate from 0 to target.
    Check aria-labels with browser accessibility inspector.
    Mobile: 2 columns. Desktop: 4 columns.
  </verify>
  <done>
    ImpactStats.tsx displays 4 statistics on a primary-900 dark background. Numbers animate from 0 to their target value when scrolled into view. Animation fires once. Reduced motion shows final values immediately. Grid is responsive (2 cols mobile, 4 cols desktop). Stats have proper aria-labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build FeaturedTestimonial and EventsPreview components</name>
  <files>
    src/components/home/FeaturedTestimonial.tsx
    src/components/home/EventsPreview.tsx
  </files>
  <action>
    **FeaturedTestimonial.tsx (HOME-05):**

    - `"use client"` for Motion animations
    - Import `testimonials` from `@/data/testimonials`
    - Select the first testimonial (farmer -- James) as the featured one. Can also accept an optional `index` prop to display a different one.

    **Background:** stone-100 (alternating rhythm after the dark stats section).

    **Layout:**
    - Section: py-16 md:py-24, bg-stone-100
    - Content: max-w-4xl mx-auto, px-4, text-center

    **Content:**
    - Large decorative opening quote mark: a gold `"` character rendered large (text-8xl or bigger, text-accent-500, opacity-30 or so, font-heading), positioned above the quote. Alternatively, use an SVG quotation mark in gold. This is the signature visual element.
    - The quote text: font-heading, text-2xl md:text-3xl, font-normal (weight 400), text-text-primary, italic, leading-relaxed. Max-width ~prose (42rem).
    - Attribution below: "-- [name], [identifier]" in font-body, text-base, text-text-muted, mt-6. The dash should be an em-dash.
    - Optional: a thin gold accent line below the attribution, centered, 40px wide

    **Animation:**
    - Section fades up when in view (whileInView, once)
    - Quote mark fades in with slight scale (0.8 -> 1.0), 600ms
    - Quote text fades up, 600ms, 200ms delay
    - Attribution fades up, 600ms, 400ms delay
    - Respect reduced motion

    **Accessibility:**
    - Wrap in `<blockquote>` with `<cite>` for attribution
    - Section `aria-label="Testimonial"`

    ---

    **EventsPreview.tsx (HOME-06):**

    - `"use client"` for Motion animations
    - Import `events` from `@/data/events`
    - Import `Link` from `next/link`

    **Background:** stone-50 (alternating rhythm).

    **Layout:**
    - Section: py-16 md:py-24, bg-stone-50
    - Content: max-w-6xl mx-auto, px-4
    - Overline: "UPCOMING EVENTS" (uppercase, tracking-widest, text-accent-600, text-sm, font-bold, text-center, mb-3)
    - Heading: "What's Coming Up" as h2, font-heading, text-4xl, text-center, mb-4
    - Brief intro: "Join us at our upcoming events and workshops" -- text-center, text-text-muted, mb-12

    **Event cards (show 2-3 events):**
    - Sort events by date ascending, take the first 3
    - Display as a responsive grid: grid-cols-1 md:grid-cols-3, gap-6

    Each event card:
    - bg-white rounded-lg overflow-hidden shadow-sm
    - Top section: a colored date badge area (bg-primary-800 text-white p-4) showing:
      - Day of month (large, text-3xl, font-heading, font-bold)
      - Month abbreviation (text-sm, uppercase)
    - Body: p-5
      - Format badge: small pill/tag showing "In-Person" / "Virtual" / "Hybrid" (text-xs, rounded-full, px-3 py-1, bg-accent-100 text-accent-600 for in-person, bg-primary-100 text-primary-700 for virtual, bg-stone-200 text-text-body for hybrid)
      - Title: font-heading, text-xl, font-semibold, mt-3
      - Time + Location: text-sm, text-text-muted, mt-2, flex items-center gap-1 with Clock and MapPin icons from lucide-react
    - Hover: same card lift pattern (translateY(-4px), shadow-md)
    - Each card links to `/events/${event.slug}`

    **"View All Events" link:**
    - Centered below the cards, mt-10
    - Next.js Link to `/events`
    - Styled as a text link: text-accent-600 font-medium hover:text-accent-500, with an arrow icon (ArrowRight from lucide-react) that shifts right 4px on hover
    - Or styled as a secondary button (border style)

    **Animation:**
    - Cards stagger fade-up with 120ms delay, whileInView, once
    - Respect reduced motion

    **Date formatting:**
    - Use `Intl.DateTimeFormat('en-GB', { day: 'numeric', month: 'short' })` for the date badge
    - Use `Intl.DateTimeFormat('en-GB', { hour: '2-digit', minute: '2-digit' })` for time display
  </action>
  <verify>
    FeaturedTestimonial: renders blockquote with gold quote mark, attribution shows name and identifier.
    EventsPreview: 3 event cards in a row on desktop, 1 column mobile. Each has date badge, format tag, title, time/location. "View All Events" link points to /events.
    Hover effects work on event cards.
  </verify>
  <done>
    FeaturedTestimonial.tsx renders the farmer testimonial in a blockquote with decorative gold quote marks and proper cite attribution. EventsPreview.tsx shows the next 3 upcoming events sorted by date, each with a date badge, format tag, title, and time/location. "View All Events" link routes to /events. Both components animate on scroll and respect reduced motion.
  </done>
</task>

</tasks>

<verification>
- All 3 component files exist at specified paths
- `npx tsc --noEmit` passes
- ImpactStats counters animate from 0 to target on scroll
- FeaturedTestimonial uses semantic `<blockquote>` and `<cite>`
- EventsPreview sorts events by date and shows only 3
- All components import from `@/data/` (not hardcoded content)
- All animations use `motion/react` and respect `useReducedMotion()`
- All interactive elements use Next.js `Link` for internal routes
</verification>

<success_criteria>
- HOME-04 (Impact Stats): 4 animated counters on dark background, responsive 2x4 grid
- HOME-05 (Testimonial): Blockquote with gold accents on stone-100 background
- HOME-06 (Events Preview): 3 event cards + "View All Events" link, sorted by date
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-02-SUMMARY.md`
</output>
