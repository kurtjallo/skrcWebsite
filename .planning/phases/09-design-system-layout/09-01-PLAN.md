---
phase: 09-design-system-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/lib/fonts.ts
  - src/app/layout.tsx
  - src/components/layout/SectionWrapper.tsx
  - src/components/shared/CornerBrackets.tsx
autonomous: true

must_haves:
  truths:
    - "All gold/antique-gold accent colors are replaced with royal blue across the entire site"
    - "Page background is light blue-grey (#f0f1f8), not warm stone"
    - "Cormorant Garamond italic renders as true italic (calligraphic), not faux-italic"
    - "Focus rings are royal blue, not gold"
    - "No gold hex values (#C9A227, #A8871E, #D4B545, etc.) remain anywhere in src/"
  artifacts:
    - path: "src/app/globals.css"
      provides: "v2.0 color tokens, shadcn variables, focus ring"
      contains: "--color-accent-500: #3b3bff"
    - path: "src/lib/fonts.ts"
      provides: "Cormorant Garamond with explicit italic style loading"
      contains: 'style: ["normal", "italic"]'
    - path: "src/components/layout/SectionWrapper.tsx"
      provides: "Updated background rhythm for v2.0 (surface-page based)"
      contains: "surface-page"
  key_links:
    - from: "src/app/globals.css"
      to: "all components"
      via: "Tailwind utility classes (accent-500, primary-900, etc.)"
      pattern: "--color-accent-500"
    - from: "src/app/globals.css :root block"
      to: "shadcn/ui components (Button, Card, inputs)"
      via: "CSS custom properties (--ring, --accent, --primary)"
      pattern: "--ring: #3b3bff"
---

<objective>
Replace the entire v1.0 gold/warm-stone visual foundation with v2.0 navy/blue tokens.

Purpose: This is the single most impactful change in Phase 9 -- every component that references accent-*, stone-*, or primary-* tokens will visually update when these values change. This plan establishes the color, typography, and background foundation that all subsequent plans depend on.

Output: Updated globals.css (@theme + :root), fonts.ts with italic loading, SectionWrapper with new backgrounds, layout.tsx body class update, CornerBrackets hardcoded hex fix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-layout/09-RESEARCH.md
@mockups/homepage-reference.md (Global Design Language section -- color palette)

@src/app/globals.css
@src/lib/fonts.ts
@src/app/layout.tsx
@src/components/layout/SectionWrapper.tsx
@src/components/shared/CornerBrackets.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite color tokens and shadcn variables in globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    Replace the ENTIRE @theme color block with the v2.0 palette. Key changes:

    **@theme block replacements:**
    - Primary: Replace slate navy (#0F1A2E, #1B2A4A, #243658, #2E4A6E, #3D5F8A) with deep navy (#121240, #1a1a5e, #252580, #2e2ea0, #3535c0)
    - Accent: Replace antique gold (#A8871E, #C9A227, #D4B545, #E2CB6E, #F0E1A0, #FAF3D8) with royal blue (#3030e0, #3b3bff, #5c5cff, #8080ff, #b0b0ff, #e8e8ff)
    - Remove secondary-* tokens entirely (heather moor #5B4A5E family -- not used in v2.0)
    - Replace stone-* tokens with blue-grey equivalents: stone-50 -> #f0f1f8, stone-100 -> #e8e9f2, stone-200 -> #e0e2ea, stone-300 -> #d0d2dc, stone-400 -> #a0a2b0
    - Add new surface tokens: --color-surface-page: #f0f1f8, --color-surface-card: #ffffff, --color-surface-dark: #121240
    - Add new divider token: --color-divider: #e0e2ea
    - Update text tokens: text-primary -> #1a1a5e, text-body -> #4a4a5a, text-muted -> #6a6a7a, text-on-dark -> #ffffff

    **:root block replacements (shadcn/ui variables):**
    - --radius: 0.75rem (slightly larger for v2.0 card radius)
    - --background: #f0f1f8
    - --foreground: #1a1a5e
    - --card: #ffffff
    - --card-foreground: #1a1a5e
    - --popover: #ffffff
    - --popover-foreground: #1a1a5e
    - --primary: #1a1a5e
    - --primary-foreground: #ffffff
    - --secondary: #e8e8ff (repurpose to blue tint)
    - --secondary-foreground: #3b3bff
    - --muted: #e8e9f2
    - --muted-foreground: #6a6a7a
    - --accent: #e8e8ff
    - --accent-foreground: #3b3bff
    - --border: #e0e2ea
    - --input: #e0e2ea
    - --ring: #3b3bff
    - Update chart-* colors to navy/blue family
    - Update sidebar-* colors to match new palette

    **@layer base body rule:**
    - Change `background-color: var(--color-stone-50)` to `background-color: var(--color-surface-page)`

    **Focus-visible rule:**
    - `outline: 2px solid var(--color-accent-500)` stays the same (accent-500 is now blue, so it auto-updates)

    Keep semantic tokens (--color-success, --color-error) unchanged.
    Keep typography scale unchanged.
    Keep --ease-premium unchanged.
    Keep font-family definitions unchanged.
    Keep @theme inline block for shadcn radius/color bridging unchanged (it reads from :root vars which we update).
    Keep all @media rules (reduced-motion, coarse pointer) unchanged.
  </action>
  <verify>
    1. Run `grep -r '#C9A227\|#A8871E\|#D4B545\|#E2CB6E\|#F0E1A0\|#FAF3D8' src/app/globals.css` -- should return nothing
    2. Run `grep 'accent-500' src/app/globals.css` -- should show #3b3bff
    3. Run `grep 'secondary-' src/app/globals.css` -- should NOT show heather moor hex values (only the shadcn --secondary variable with new blue value)
    4. Run `grep '#FAFAF7\|#F5F2EC' src/app/globals.css` -- should return nothing (old stone colors gone)
    5. Run `grep 'surface-page' src/app/globals.css` -- should show #f0f1f8
    6. Run `npm run build` passes (no Tailwind compilation errors)
  </verify>
  <done>globals.css @theme block has all v2.0 navy/blue tokens, :root has updated shadcn variables, no gold hex values remain, no warm stone hex values remain, surface tokens defined</done>
</task>

<task type="auto">
  <name>Task 2: Update font loading, layout body class, SectionWrapper, and CornerBrackets</name>
  <files>src/lib/fonts.ts, src/app/layout.tsx, src/components/layout/SectionWrapper.tsx, src/components/shared/CornerBrackets.tsx</files>
  <action>
    **fonts.ts:**
    Add `style: ["normal", "italic"]` to the Cormorant_Garamond config to ensure true italic glyphs are loaded (not faux italic). The updated call:
    ```ts
    export const cormorantGaramond = Cormorant_Garamond({
      subsets: ["latin"],
      weight: ["300", "400", "600"],
      style: ["normal", "italic"],
      variable: "--font-cormorant-garamond",
      display: "swap",
    });
    ```

    **layout.tsx:**
    Change the body className from `bg-stone-50` to `bg-surface-page` so the page background uses the new surface token. Keep all other classes unchanged.

    **SectionWrapper.tsx:**
    The v2.0 design uses a FLAT page background (surface-page / #f0f1f8) with white cards providing contrast, rather than the v1.0 alternating stone-50/stone-100 bands. Update:
    - Change AUTO_BACKGROUNDS array to: `["bg-surface-page", "bg-surface-page", "bg-surface-page", "bg-surface-dark text-white", "bg-surface-page"]`
    - Update light-a variant to `"bg-surface-page"`
    - Update light-b variant to `"bg-surface-card"` (white)
    - Update dark variant to `"bg-surface-dark text-white"` (was `bg-primary-900 text-stone-100`)

    **CornerBrackets.tsx:**
    Replace the hardcoded `const GOLD_COLOR = "#C9A227"` with `const ACCENT_COLOR = "#3b3bff"` and update the default value of the `color` prop from `GOLD_COLOR` to `ACCENT_COLOR`. This is the ONLY hardcoded gold hex in the codebase.
  </action>
  <verify>
    1. Run `grep -r '#C9A227' src/` -- should return ZERO results (CornerBrackets was the only hardcoded gold)
    2. Run `grep 'style:' src/lib/fonts.ts` -- should show `["normal", "italic"]`
    3. Run `grep 'bg-surface-page' src/app/layout.tsx` -- should find the body class
    4. Run `grep 'surface-page\|surface-card\|surface-dark' src/components/layout/SectionWrapper.tsx` -- should show new background tokens
    5. Run `npm run build` passes
  </verify>
  <done>Font italic explicitly loaded, body uses surface-page bg, SectionWrapper uses flat v2.0 backgrounds, no hardcoded gold hex values remain in src/</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn '#C9A227\|#A8871E\|#D4B545\|#E2CB6E\|#F0E1A0\|#FAF3D8\|GOLD' src/` -- zero results (complete gold elimination)
2. `grep -rn '#FAFAF7\|#F5F2EC\|#EDE8DF\|#DDD6CA\|#B8AFA1' src/app/globals.css` -- zero results (old stone colors gone)
3. `npm run build` succeeds with no errors
4. Dev server renders pages with blue accents instead of gold, light blue-grey backgrounds instead of warm stone
</verification>

<success_criteria>
- Every accent-* token resolves to royal blue (#3b3bff family)
- Every primary-* token resolves to deep navy (#1a1a5e family)
- Page background is #f0f1f8 (light blue-grey)
- Cards are #ffffff (white)
- Focus rings are #3b3bff (royal blue)
- Cormorant Garamond italic style is explicitly loaded
- No gold hex values exist anywhere in src/
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-layout/09-01-SUMMARY.md`
</output>
