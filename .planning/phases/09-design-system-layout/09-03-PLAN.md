---
phase: 09-design-system-layout
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/button.tsx
  - src/components/layout/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "Cards have white background, rounded-xl corners, no visible border, hover lift with shadow (no gold top border)"
    - "Button 'gold' and 'gold-outline' variants are removed; remaining variants use v2.0 color tokens"
    - "Footer has dark navy background with 4-column grid: brand description, menu links, social links, contact info"
    - "Footer bottom bar shows copyright and charity number"
    - "All footer links use tel: for phone and mailto: for email"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "v2.0 white card with hover lift + shadow (DS-05)"
      exports: ["Card", "CardHeader", "CardTitle", "CardDescription", "CardContent", "CardFooter"]
    - path: "src/components/ui/button.tsx"
      provides: "Button without gold variants"
      exports: ["Button", "buttonVariants"]
    - path: "src/components/layout/Footer.tsx"
      provides: "Dark navy 4-column footer (LAYOUT-03)"
      exports: ["Footer"]
      min_lines: 80
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "globals.css :root --card variable"
      via: "Tailwind bg-card class"
      pattern: "bg-card"
    - from: "src/components/layout/Footer.tsx"
      to: "src/lib/constants.ts"
      via: "NAV_ITEMS, SITE_CONFIG, SOCIAL_LINKS imports"
      pattern: "import.*from.*@/lib/constants"
---

<objective>
Update the existing Card and Button components to v2.0 patterns, and rewrite the Footer to match the new dark navy design.

Purpose: Card and Button are used across every page -- updating their patterns here means downstream page redesigns get the correct visual treatment automatically. The Footer appears on every page and must match the new design language immediately.

Output: Updated card.tsx (white + shadow hover), updated button.tsx (gold variants removed), rewritten Footer.tsx (dark navy 4-column).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-layout/09-RESEARCH.md (Card, Button, Footer sections)
@mockups/homepage-reference.md (Card Pattern, Section 11: Footer)

@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/components/layout/Footer.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Card component to v2.0 white card pattern</name>
  <files>src/components/ui/card.tsx</files>
  <action>
    Update the Card component to match v2.0 design: white background, rounded corners, no visible border, hover lift with shadow.

    **Base card changes:**
    - Change from `bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm` to `bg-card text-card-foreground flex flex-col gap-6 rounded-xl py-6` (remove `border` and `shadow-sm` from base -- v2.0 cards are cleaner with no border and no default shadow)

    **Hoverable card changes:**
    - Remove the gold top-border scaleX animation entirely: delete the `before:content-['']`, `before:absolute`, `before:top-0`, `before:left-0`, `before:w-full`, `before:h-[2px]`, `before:bg-accent-500`, `before:scale-x-0`, `before:origin-left`, `before:transition-transform`, `before:duration-400`, `before:ease-premium`, `hover:before:scale-x-100` classes
    - Keep: `group relative overflow-hidden`, `transition-all duration-300 ease-premium`, `hover:-translate-y-1 hover:shadow-lg`
    - The v2.0 hover is SIMPLER: just lift (-translate-y-1) and shadow increase (shadow-lg). No colored border animation.

    **Keep all sub-components unchanged:** CardHeader, CardTitle, CardDescription, CardContent, CardFooter retain their current implementation. They use Tailwind classes that already work with the new token values.

    **Do NOT add a `variant` prop for stats cards yet** -- DS-06 (stats card with left border accent) will be handled as a className override in the consuming component, not as a Card variant. The base Card just needs to be a clean white surface.
  </action>
  <verify>
    1. `grep 'before:bg-accent' src/components/ui/card.tsx` -- should return nothing (gold border removed)
    2. `grep 'hover:-translate-y-1' src/components/ui/card.tsx` -- should exist (hover lift kept)
    3. `grep 'border' src/components/ui/card.tsx` -- should NOT appear in the base Card class list (only in sub-components if any)
    4. `npm run build` passes
  </verify>
  <done>Card is white bg, rounded-xl, no border, hoverable variant has clean lift + shadow (no gold top border)</done>
</task>

<task type="auto">
  <name>Task 2: Remove gold button variants and clean up Button</name>
  <files>src/components/ui/button.tsx</files>
  <action>
    Remove the v1.0 gold-specific button variants and the gold-specific rendering logic.

    **Variant removals:**
    - Delete the `gold` variant entirely (the one with `bg-primary-900 text-stone-50` and the sweep animation)
    - Delete the `gold-outline` variant entirely (the one with `border-2 border-accent-500 text-accent-600` and sweep)

    **Rendering logic cleanup:**
    - Remove the `isGoldVariant` check: `const isGoldVariant = variant === "gold" || variant === "gold-outline"`
    - Remove the conditional span wrapper: change `{isGoldVariant ? <span>{children}</span> : children}` to just `{children}`

    **Keep ALL other variants unchanged:**
    - default, destructive, outline, secondary, ghost, link -- these all work fine with updated token values from Plan 01

    **Update text references to stone-50 if any remain:**
    - The gold variant had `text-stone-50` -- since we removed it, this should be gone. But check the remaining variants. The `default` variant uses `text-primary-foreground` which is correct (now maps to #ffffff via :root). No changes needed to remaining variants.

    **Why not add new variants here:** The CircleArrowCTA (from Plan 02) is a SEPARATE component, not a Button variant. The v2.0 design doesn't use filled rectangular buttons much -- the circle-arrow pattern replaces them. The existing Button variants (default, outline, secondary, ghost, link) remain for form submit buttons and utility uses. A new `nav-outline` variant for the header "Contact" button may be needed in Plan 04 but is NOT part of this task.
  </action>
  <verify>
    1. `grep -c 'gold' src/components/ui/button.tsx` -- should return 0
    2. `grep 'isGoldVariant' src/components/ui/button.tsx` -- should return nothing
    3. `grep 'stone-50' src/components/ui/button.tsx` -- should return nothing
    4. `grep 'sweep\|translate-x' src/components/ui/button.tsx` -- should return nothing (sweep animation removed with gold variants)
    5. `npm run build` passes
    6. Note: Pages that currently use `variant="gold"` or `variant="gold-outline"` will show TypeScript errors. This is EXPECTED -- those pages will be redesigned in Phases 10-13 and will switch to CircleArrowCTA. For now, temporarily change any gold/gold-outline usages to `variant="default"` to keep the build clean.
  </verify>
  <done>Gold and gold-outline button variants removed, sweep animation logic removed, remaining variants unchanged, build passes (gold usages in pages temporarily switched to default)</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Footer to dark navy 4-column design</name>
  <files>src/components/layout/Footer.tsx</files>
  <action>
    Rewrite the Footer to match the v2.0 design reference (Section 11: Footer).

    **Structure:** Dark navy background, 4-column grid, bottom bar.

    **Background:** Change from `bg-primary-950 text-stone-100` to `bg-primary-950 text-white` (v2.0 uses pure white text on dark backgrounds, not the warm stone-100).

    **4-column grid layout:**
    1. **Brand column (left):**
       - Org name: "St Katharine Rural Connect" in white serif (font-heading), text-xl
       - Tagline/description: `SITE_CONFIG.tagline`, text-sm, text-white/70
       - Remove the charity badge card that exists in v1.0 -- charity number moves to bottom bar
       - Optional: "Contact Us" outlined button (white border, white text, rounded, links to /contact). Use a simple styled Link, not the Button component.

    2. **Menu column:**
       - Header: "Menu" in white, font-semibold, text-sm, uppercase tracking-wider
       - Links: Use NAV_ITEMS.slice(0, 5) (Home, About, Services, Who We Serve, Events)
       - Link style: text-white/70 hover:text-white, text-sm, transition-colors

    3. **Socials column:**
       - Header: "Socials" in white, same style as Menu header
       - Links: Use SOCIAL_LINKS array (Facebook, Twitter, Instagram)
       - Render as text links (not just icons) -- show the platform name
       - Link style: same as menu links

    4. **Contact column (right):**
       - Header: "Contact" in white, same style
       - Phone: tel: link using SITE_CONFIG.phoneHref
       - Email: mailto: link
       - Office hours (text only)
       - Location (text only)
       - Use Phone, Mail, Clock, MapPin icons from lucide-react with aria-hidden="true"
       - Link/text style: text-white/70, links hover:text-white

    **Bottom bar:**
    - Thin top border: `border-t border-white/10`
    - Left: copyright + charity number: `"(c) {year} {SITE_CONFIG.name}. Scottish Charity No. {SITE_CONFIG.charityNumber}"`
    - Right: Social icon links (Facebook, Twitter, Instagram icons only, no text)
    - Text: text-white/50, text-xs
    - flex-col on mobile (sm:flex-row) with center alignment

    **Key differences from v1.0:**
    - Column headers are WHITE, not accent-500 (no blue/gold column headings)
    - Links use text-white/70 (opacity), not text-stone-300
    - Hover is text-white (full opacity), not text-accent-400
    - Charity badge is removed (charity number in bottom bar only)
    - Overall simpler and cleaner

    **Keep:** Server component, imports from @/lib/constants, all tel: and mailto: links for accessibility.
  </action>
  <verify>
    1. `grep 'accent-500\|accent-400' src/components/layout/Footer.tsx` -- should return nothing (no accent colors in footer)
    2. `grep 'stone-100\|stone-200\|stone-300\|stone-400' src/components/layout/Footer.tsx` -- should return nothing (no stone references)
    3. `grep 'SITE_CONFIG.phoneHref' src/components/layout/Footer.tsx` -- should exist (tel: link preserved)
    4. `grep 'SITE_CONFIG.charityNumber' src/components/layout/Footer.tsx` -- should exist (charity number in bottom bar)
    5. `grep 'mailto:' src/components/layout/Footer.tsx` -- should exist
    6. `grep 'aria-hidden' src/components/layout/Footer.tsx` -- should exist (decorative icons)
    7. `npm run build` passes
  </verify>
  <done>Footer displays dark navy background, 4-column grid (brand, menu, socials, contact), bottom bar with copyright and charity number, all links accessible with tel: and mailto:, no gold/stone references</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `npm run build` passes with no errors
2. No gold-related classes or hex values in any of the three files
3. Card hover shows clean lift + shadow (no colored border)
4. Button has no gold/gold-outline variants
5. Footer renders dark navy with white text, 4-column layout
</verification>

<success_criteria>
- Card: white bg, rounded-xl, no border, hover = lift + shadow only
- Button: gold and gold-outline variants deleted, build clean
- Footer: dark navy bg, 4-column grid, white text with opacity, charity number in bottom bar
- All components use v2.0 token names
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-layout/09-03-SUMMARY.md`
</output>
