---
phase: 08-cross-cutting-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/components/shared/JsonLd.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/events/[slug]/page.tsx
  - src/components/layout/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "sitemap.xml is auto-generated and lists all pages"
    - "robots.txt allows all crawlers and references sitemap"
    - "JSON-LD NGO structured data is present on every page"
    - "JSON-LD CommunityEvent structured data is present on event detail pages"
    - "Charity registration number appears in the site footer"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Auto-generated sitemap with all pages"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Robots.txt configuration"
      exports: ["default"]
    - path: "src/components/shared/JsonLd.tsx"
      provides: "Reusable JSON-LD script injection component"
      exports: ["JsonLd", "ngoSchema", "communityEventSchema"]
  key_links:
    - from: "src/app/sitemap.ts"
      to: "all pages"
      via: "URL list covers every route"
      pattern: "url:.*skrc"
    - from: "src/components/shared/JsonLd.tsx"
      to: "src/app/layout.tsx"
      via: "NGO schema injected in root layout"
      pattern: "JsonLd|application/ld\\+json"
    - from: "src/components/shared/JsonLd.tsx"
      to: "src/app/events/[slug]/page.tsx"
      via: "CommunityEvent schema injected on event detail pages"
      pattern: "communityEventSchema|CommunityEvent"
---

<objective>
Implement all SEO requirements (SEO-01 through SEO-05): auto-generated sitemap and robots.txt, JSON-LD structured data for the organization and events, and charity registration number in the footer.

Purpose: Search visibility is how rural community members discover SKRC. Proper structured data helps Google understand this is a Scottish NGO offering community support, and surfaces events in search results. The sitemap ensures all pages are discoverable by crawlers.

Output: sitemap.ts and robots.ts files, a reusable JsonLd component with NGO and CommunityEvent schemas, and charity registration number displayed in the footer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@RESEARCH.md (section 8: SEO Strategy)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sitemap, robots.txt, and charity registration number</name>
  <files>
    src/app/sitemap.ts
    src/app/robots.ts
    src/components/layout/Footer.tsx
  </files>
  <action>
**1. Create `src/app/sitemap.ts` (SEO-02):**

Use Next.js Metadata API to auto-generate the sitemap. Include every page route:

```ts
import { MetadataRoute } from "next";

// Service pillar slugs -- must match the data used in services/[slug]
const serviceSlugs = [
  "supporting-your-wellbeing",
  "supporting-farm-life",
  "supporting-those-who-work-away",
  "strengthening-our-community",
];

// Sample event slugs -- must match the data used in events/[slug]
// In production with a CMS, these would be fetched dynamically
const eventSlugs = [
  "farm-wellbeing-workshop",
  "community-coffee-morning",
  "offshore-families-support-group",
  "volunteer-orientation",
  "resilience-building-workshop",
  "summer-community-gathering",
];

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = "https://skrc.org.uk";

  const staticPages: MetadataRoute.Sitemap = [
    { url: baseUrl, lastModified: new Date(), changeFrequency: "monthly", priority: 1.0 },
    { url: `${baseUrl}/about`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.8 },
    { url: `${baseUrl}/services`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.9 },
    { url: `${baseUrl}/who-we-serve`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.8 },
    { url: `${baseUrl}/events`, lastModified: new Date(), changeFrequency: "weekly", priority: 0.8 },
    { url: `${baseUrl}/get-involved`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.7 },
    { url: `${baseUrl}/partnerships`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.6 },
    { url: `${baseUrl}/team`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.6 },
    { url: `${baseUrl}/testimonials`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.5 },
    { url: `${baseUrl}/contact`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.8 },
  ];

  const servicePages: MetadataRoute.Sitemap = serviceSlugs.map((slug) => ({
    url: `${baseUrl}/services/${slug}`,
    lastModified: new Date(),
    changeFrequency: "monthly" as const,
    priority: 0.7,
  }));

  const eventPages: MetadataRoute.Sitemap = eventSlugs.map((slug) => ({
    url: `${baseUrl}/events/${slug}`,
    lastModified: new Date(),
    changeFrequency: "weekly" as const,
    priority: 0.6,
  }));

  return [...staticPages, ...servicePages, ...eventPages];
}
```

IMPORTANT: Check the actual slug values used in `src/data/` files (e.g., `src/data/services.ts`, `src/data/events.ts`) and use those exact slugs. The slugs above are examples -- match whatever Phases 4 and 5 actually created. If the data files export arrays, import them and map to slugs dynamically:
```ts
import { services } from "@/data/services";
import { events } from "@/data/events";

// Then map: services.map(s => s.slug), events.map(e => e.slug)
```

**2. Create `src/app/robots.ts` (SEO-03):**

```ts
import { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: "*",
        allow: "/",
      },
    ],
    sitemap: "https://skrc.org.uk/sitemap.xml",
  };
}
```

**3. Ensure charity registration number in footer (SEO-05):**
- Open `src/components/layout/Footer.tsx` and verify the charity registration number (from `SITE_CONFIG.charityNumber`) is displayed.
- It should appear in the footer bottom bar: "Scottish Charity No. SC000000" (placeholder).
- If already present (added in Phase 2), verify it is visible and correct.
- If missing, add it to the copyright line: `(c) {year} St Katharine Rural Connect. Scottish Charity No. {SITE_CONFIG.charityNumber}`
- Also verify the charity number text is wrapped in a `<span>` or similar element that assistive technology can identify, not hidden or at very low contrast.
  </action>
  <verify>
  1. `ls src/app/sitemap.ts src/app/robots.ts` -- both files exist
  2. `grep "MetadataRoute.Sitemap" src/app/sitemap.ts` -- uses correct Next.js type
  3. `grep "skrc.org.uk" src/app/sitemap.ts` -- has correct base URL
  4. `grep "sitemap" src/app/robots.ts` -- references sitemap URL
  5. `grep -c "url:" src/app/sitemap.ts` -- returns 10+ (all static pages plus dynamic)
  6. `grep "charityNumber\|SC0" src/components/layout/Footer.tsx` -- charity number displayed
  7. `npm run build` -- succeeds (confirms sitemap.ts and robots.ts compile)
  </verify>
  <done>
  - SEO-02: sitemap.ts auto-generates sitemap covering all static pages, service pillar pages, and event pages
  - SEO-03: robots.ts allows all crawlers and points to sitemap.xml
  - SEO-05: Charity registration number displayed in footer
  </done>
</task>

<task type="auto">
  <name>Task 2: JSON-LD structured data -- NGO organization schema and CommunityEvent schema</name>
  <files>
    src/components/shared/JsonLd.tsx
    src/app/layout.tsx
    src/app/events/[slug]/page.tsx
  </files>
  <action>
**1. Create `src/components/shared/JsonLd.tsx` (SEO-04):**

Build a reusable component for injecting JSON-LD structured data, plus pre-built schemas for the organization and events:

```tsx
import { SITE_CONFIG } from "@/lib/constants";

// Generic JSON-LD script injector
export function JsonLd({ data }: { data: Record<string, unknown> }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  );
}

// NGO Organization schema -- use on every page (via root layout)
export function ngoSchema() {
  return {
    "@context": "https://schema.org",
    "@type": "NGO",
    "name": "St Katharine Rural Connect",
    "alternateName": "SKRC",
    "description": SITE_CONFIG.tagline,
    "url": "https://skrc.org.uk",
    "telephone": SITE_CONFIG.phoneHref.replace("tel:", ""),
    "email": SITE_CONFIG.email,
    "areaServed": {
      "@type": "Place",
      "name": "Rural Scotland",
    },
    "serviceType": [
      "Community Wellness and Mental Health",
      "Agricultural Support and Resources",
      "Offshore Worker Engagement and Support",
      "Community Connection and Development",
    ],
    "sameAs": [
      // Social media URLs would go here when available
    ],
  };
}

// CommunityEvent schema -- use on individual event detail pages
export function communityEventSchema({
  name,
  description,
  startDate,
  endDate,
  location,
  eventAttendanceMode,
  url,
}: {
  name: string;
  description: string;
  startDate: string; // ISO 8601 format
  endDate?: string;
  location: string;
  eventAttendanceMode: "online" | "offline" | "mixed";
  url: string;
}) {
  const attendanceModeMap = {
    online: "https://schema.org/OnlineEventAttendanceMode",
    offline: "https://schema.org/OfflineEventAttendanceMode",
    mixed: "https://schema.org/MixedEventAttendanceMode",
  };

  return {
    "@context": "https://schema.org",
    "@type": "CommunityEvent",
    "name": name,
    "description": description,
    "startDate": startDate,
    ...(endDate && { "endDate": endDate }),
    "location": {
      "@type": eventAttendanceMode === "online" ? "VirtualLocation" : "Place",
      "name": location,
      ...(eventAttendanceMode === "online" && { "url": url }),
    },
    "eventAttendanceMode": attendanceModeMap[eventAttendanceMode],
    "organizer": {
      "@type": "NGO",
      "name": "St Katharine Rural Connect",
      "url": "https://skrc.org.uk",
    },
    "url": url,
  };
}
```

**2. Add NGO JSON-LD to root layout (`src/app/layout.tsx`):**

Import the JsonLd component and ngoSchema function, and add it inside the `<head>` or `<body>` of the root layout. In Next.js App Router, JSON-LD scripts are best placed in the body:

```tsx
import { JsonLd, ngoSchema } from "@/components/shared/JsonLd";

// Inside the RootLayout return, add before or after {children}:
<JsonLd data={ngoSchema()} />
```

This ensures the NGO schema appears on every page. Place it just before `{children}` inside the `<body>` element.

**3. Add CommunityEvent JSON-LD to event detail pages (`src/app/events/[slug]/page.tsx`):**

In the event detail page, import the JsonLd component and communityEventSchema function. After loading the event data (from the data file), render the schema:

```tsx
import { JsonLd, communityEventSchema } from "@/components/shared/JsonLd";

// Inside the page component, after looking up the event:
export default async function EventDetailPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const event = events.find(e => e.slug === slug);
  // ... existing rendering logic ...

  return (
    <>
      <JsonLd
        data={communityEventSchema({
          name: event.title,
          description: event.description,
          startDate: event.date, // Ensure this is ISO 8601 format
          location: event.location,
          eventAttendanceMode: event.format === "virtual" ? "online" : event.format === "hybrid" ? "mixed" : "offline",
          url: `https://skrc.org.uk/events/${slug}`,
        })}
      />
      {/* ... rest of existing page content ... */}
    </>
  );
}
```

Adapt the property names (event.title, event.date, event.location, event.format) to match the actual data structure from Phase 5. Check `src/data/events.ts` for the exact field names.

IMPORTANT: The event date in the data file must be in ISO 8601 format (e.g., "2026-06-15T10:00:00+01:00") for the schema to be valid. If dates are stored as plain strings like "15 June 2026", you will need to either:
a. Convert them to ISO format in the schema function, or
b. Add an isoDate field to the event data
  </action>
  <verify>
  1. `grep "application/ld+json" src/components/shared/JsonLd.tsx` -- JSON-LD script tag present
  2. `grep "NGO" src/components/shared/JsonLd.tsx` -- NGO schema type present
  3. `grep "CommunityEvent" src/components/shared/JsonLd.tsx` -- CommunityEvent schema present
  4. `grep "JsonLd" src/app/layout.tsx` -- NGO schema used in root layout
  5. `grep "JsonLd\|communityEventSchema" src/app/events/\[slug\]/page.tsx` -- event schema used in detail pages
  6. `npm run build` -- succeeds with no errors
  7. After build, check a page's HTML output for the JSON-LD script: `curl -s http://localhost:3000 | grep "application/ld+json"` -- returns the NGO schema
  </verify>
  <done>
  - SEO-04: JSON-LD NGO structured data present on every page via root layout
  - SEO-04: JSON-LD CommunityEvent structured data present on each event detail page
  - Reusable JsonLd component available for future schema types (FAQPage, etc.)
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. `ls src/app/sitemap.ts src/app/robots.ts src/components/shared/JsonLd.tsx` -- all three new files exist
3. `npm run dev` then `curl -s http://localhost:3000/sitemap.xml | head -20` -- returns valid XML with page URLs
4. `npm run dev` then `curl -s http://localhost:3000/robots.txt` -- returns rules with sitemap reference
5. `curl -s http://localhost:3000 | grep "application/ld+json"` -- returns NGO schema
6. `grep "charityNumber\|SC0" src/components/layout/Footer.tsx` -- charity number in footer
7. All SEO requirements (SEO-01 through SEO-05) verifiable
</verification>

<success_criteria>
All 5 SEO requirements (SEO-01 through SEO-05) satisfied:
- SEO-01: Every page has unique metadata via generateMetadata or metadata export (covered by Plan 01 A11Y-02 audit which ensures metadata exists; this plan adds SEO-specific enrichment)
- SEO-02: sitemap.ts auto-generates valid sitemap.xml with all pages
- SEO-03: robots.ts configures crawler access and references sitemap
- SEO-04: JSON-LD structured data (NGO on every page, CommunityEvent on event detail pages)
- SEO-05: Charity registration number displayed in footer
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-cutting-quality/08-02-SUMMARY.md`
</output>
