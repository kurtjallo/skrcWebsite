---
phase: 06-community-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/about/page.tsx
autonomous: true

must_haves:
  truths:
    - "About page displays founding intention as a narrative section about St Katharine's heritage"
    - "Mission, Vision, and Values are clearly displayed with values rendered as a card grid"
    - "'Why the Name Matters' section explains both 'St Katharine' and 'Rural Connect' components"
    - "Page uses SectionWrapper with alternating backgrounds for visual rhythm"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "Complete About page with 3 sections: founding, mission/values, name explanation"
      min_lines: 100
      contains: "generateMetadata"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "src/components/layout/SectionWrapper"
      via: "imports SectionWrapper for alternating backgrounds"
      pattern: "import.*SectionWrapper"
    - from: "src/app/about/page.tsx"
      to: "src/lib/metadata.ts"
      via: "uses createMetadata for SEO"
      pattern: "import.*createMetadata"
---

<objective>
Build the About page with three content sections: founding intention, mission/vision/values, and "Why the Name Matters".

Purpose: The About page establishes trust and conveys SKRC's identity and heritage. It is content-heavy with no data file dependency, making it independent of the data layer plan.

Output: A complete About page at `/about` with premium layout, alternating section backgrounds, and all ABOUT-01 through ABOUT-03 requirements satisfied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@RESEARCH.md (sections 2.1 color, 2.2 typography, 5.1 voice/tone)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the About page with founding intention, mission/values, and name explanation</name>
  <files>src/app/about/page.tsx</files>
  <action>
Create `src/app/about/page.tsx` as a React Server Component (no "use client"). This page has NO data file dependency -- all content is inline since it's static narrative content that won't change frequently.

**SEO metadata:**
Use `generateMetadata` (or export `metadata`) with:
- title: "About Us | St Katharine Rural Connect"
- description: "Learn about St Katharine Rural Connect's founding intention, mission, vision, and values. Rooted in the spirit of service, we strengthen rural communities through support, connection, and care."
- Use `createMetadata` helper from `@/lib/metadata` if available, otherwise export a static `metadata` object.

**Page structure (4 sections using SectionWrapper):**

**Section 1 -- Page Header (stone-50 background):**
- Overline text: "ABOUT US" in `text-overline font-bold uppercase tracking-widest text-accent-600 font-body`
- H1: "Rooted in Service, Growing with Community" in `font-heading text-5xl font-semibold text-text-primary`
- Intro paragraph: "St Katharine Rural Connect is a community-rooted initiative strengthening social, emotional, and practical support systems for rural people and places across Scotland." in `text-lg text-text-body max-w-prose` (max-w-[42rem] for optimal line length)

**Section 2 -- Founding Intention (stone-100 background):** [ABOUT-01]
- Use a two-column layout on desktop (`grid grid-cols-1 lg:grid-cols-2 gap-12 items-center`), stacking on mobile.
- Left column: Content
  - H2: "Our Founding Intention" in `font-heading text-4xl font-semibold text-text-primary`
  - Two paragraphs of narrative content:
    - Paragraph 1: "St Katharine Rural Connect carries forward the spirit and values of St Katharine -- a legacy of service, learning, dignity, and community care. Our founding grew from a simple observation: rural communities across Scotland were quietly carrying immense pressures, from the isolation of farming life to the strain of offshore work, with few places to turn for understanding and support."
    - Paragraph 2: "We were established to change that. Not with grand programmes delivered from a distance, but with genuine, grounded support that meets people where they are. We believe that meaningful change happens through trust, presence, and practical care -- the kind a good neighbour provides, backed by the knowledge and resources to make a real difference."
  - Text styling: `text-base text-text-body leading-relaxed` with `space-y-6` between paragraphs
- Right column: A decorative placeholder (since no images yet). Create a `div` with `bg-stone-200 rounded-lg aspect-[4/3] flex items-center justify-center`. Inside, show a subtle icon or text: "Landscape imagery coming soon" in `text-text-muted text-sm italic`. Add a gold accent line at the top: `border-t-2 border-accent-500`.

**Section 3 -- Mission, Vision & Values (stone-50 background):** [ABOUT-02]
- H2: "What Guides Us" in `font-heading text-4xl font-semibold text-text-primary text-center`

- **Mission subsection:**
  - H3: "Our Mission" in `font-heading text-3xl font-semibold text-text-primary`
  - Text: "Support and strengthen rural communities by providing accessible, compassionate, and practical services that foster wellbeing, resilience, and connection."
  - Style as a featured block: `bg-stone-100 rounded-lg p-8 border-l-4 border-accent-500` with text in `text-lg text-text-body italic`

- **Vision subsection:**
  - H3: "Our Vision" in `font-heading text-3xl font-semibold text-text-primary`
  - Text: "Thriving rural communities where every person has access to support, relationships, and opportunities to flourish."
  - Same featured block style as Mission

- **Values grid:**
  - H3: "Our Values" in `font-heading text-3xl font-semibold text-text-primary text-center`
  - 7 values displayed as a responsive card grid: `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4`
  - Each value card: `bg-white rounded-lg p-6 text-center shadow-sm border border-stone-200 hover:shadow-md hover:border-accent-300 transition-all duration-300 ease-premium`
  - Value name in `font-heading text-xl font-semibold text-primary-900`
  - Each value gets a one-line description below the name in `text-sm text-text-muted mt-2`:
    - Compassion: "Meeting people with kindness and understanding"
    - Connection: "Building bridges between people and support"
    - Respect: "Honouring every person's dignity and story"
    - Integrity: "Being honest, transparent, and trustworthy"
    - Accessibility: "Removing barriers to support and belonging"
    - Collaboration: "Working together for stronger communities"
    - Resilience: "Nurturing the strength within people and places"

**Section 4 -- Why the Name Matters (primary-900 dark background):** [ABOUT-03]
- This is the dramatic dark break section per the design system rhythm.
- H2: "Why the Name Matters" in `font-heading text-4xl font-semibold text-text-on-dark text-center`
- Two-column layout on desktop (`grid grid-cols-1 md:grid-cols-2 gap-12`):
  - Left card: "St Katharine" explanation
    - Decorative gold accent line at top: `border-t-2 border-accent-500 pt-6`
    - H3: "St Katharine" in `font-heading text-3xl font-semibold text-accent-400`
    - Text: "The name St Katharine represents our foundation -- a legacy of service, care, and values-based community work. It speaks to the deep roots from which this initiative has grown, and the enduring commitment to dignity and learning that guides everything we do." in `text-base text-text-on-dark leading-relaxed`
  - Right card: "Rural Connect" explanation
    - Same decorative gold accent line at top
    - H3: "Rural Connect" in `font-heading text-3xl font-semibold text-accent-400`
    - Text: "Rural Connect describes our mission -- connecting people to services, to neighbours, to support, to opportunities, and to the resources they need. It's about closing the gaps that rural life can create, and ensuring no one has to face their challenges alone." in `text-base text-text-on-dark leading-relaxed`
- Below the two columns, centered: A unifying statement: "Together, the name reflects who we are: a trusted, values-driven organisation dedicated to strengthening rural communities through genuine connection and care." in `text-lg text-stone-300 text-center max-w-prose mx-auto mt-8 italic`

**General rules:**
- Import `SectionWrapper` from `@/components/layout/SectionWrapper` (or equivalent -- if it's not available yet, wrap sections in `<section>` with appropriate background classes directly). The SectionWrapper is defined in Phase 2; if the component doesn't exist yet when executing, use raw `<section className="py-16 px-6 bg-{color}">` with a centered `<div className="mx-auto max-w-7xl">` inside.
- Use semantic HTML: `<main id="main-content">`, `<section>`, proper heading hierarchy (h1 > h2 > h3, never skip).
- All text uses the voice/tone from CLAUDE.md: warm, personal, grounded, "trusted neighbour" voice.
- Do NOT use em dashes in the actual code -- use the unicode character directly or the HTML entity.
  </action>
  <verify>
- `grep "generateMetadata\|export const metadata" src/app/about/page.tsx` confirms SEO metadata
- `grep "Founding Intention\|Our Founding" src/app/about/page.tsx` confirms ABOUT-01
- `grep "Mission\|Vision\|Values" src/app/about/page.tsx` confirms ABOUT-02
- `grep "Why the Name Matters" src/app/about/page.tsx` confirms ABOUT-03
- `grep "St Katharine.*Rural Connect\|Rural Connect.*St Katharine" src/app/about/page.tsx` confirms name explanation
- `grep "<h1\|<h2\|<h3" src/app/about/page.tsx` shows proper heading hierarchy (1 h1, multiple h2, multiple h3)
- `grep "primary-900\|stone-100\|stone-50" src/app/about/page.tsx` confirms alternating backgrounds
- `npm run build` passes
  </verify>
  <done>
- ABOUT-01: Founding intention section with two-column layout and narrative content
- ABOUT-02: Mission (bordered block), Vision (bordered block), and 7 Values (card grid with descriptions and hover effects)
- ABOUT-03: "Why the Name Matters" on dark primary-900 background with two-column gold-accented explanation of "St Katharine" and "Rural Connect"
- Page has proper SEO metadata, semantic HTML, heading hierarchy, and alternating section backgrounds
  </done>
</task>

</tasks>

<verification>
1. `ls src/app/about/page.tsx` -- file exists
2. Visit `/about` -- page renders with 4 distinct sections, alternating backgrounds
3. Content check: founding story, mission, vision, 7 values, name explanation all present
4. Dark section (primary-900) for "Why the Name Matters" creates visual drama
5. Values render as hoverable cards in a responsive grid
6. `npm run build` passes without errors
</verification>

<success_criteria>
- About page renders at `/about` with all 3 requirements (ABOUT-01, ABOUT-02, ABOUT-03) satisfied
- Proper heading hierarchy (h1 > h2 > h3) with no skipped levels
- Alternating section backgrounds (stone-50, stone-100, stone-50, primary-900)
- Two-column layouts on desktop, single-column on mobile
- Gold accent lines used sparingly (top borders on name explanation cards, left border on mission/vision blocks)
- Voice is warm and grounded, not corporate or institutional
</success_criteria>

<output>
After completion, create `.planning/phases/06-community-pages/06-02-SUMMARY.md`
</output>
