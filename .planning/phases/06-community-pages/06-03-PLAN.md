---
phase: 06-community-pages
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/team/TeamMemberCard.tsx
  - src/components/team/TeamGrid.tsx
  - src/app/team/page.tsx
  - src/app/testimonials/page.tsx
autonomous: true

must_haves:
  truths:
    - "Team page shows Board of Directors with name, role, bio, and rural/community connection"
    - "Team page shows Staff with name, position, bio, and area of responsibility"
    - "Team members are displayed in a consistent card-based grid layout"
    - "Testimonials page shows quotes from all 4 audience segments"
    - "Each testimonial displays first name and identifier (e.g., 'dairy farmer near Huntly')"
    - "Testimonials have visual distinction between audience segments"
  artifacts:
    - path: "src/components/team/TeamMemberCard.tsx"
      provides: "Reusable team member card with name, role, bio, connection"
      exports: ["TeamMemberCard"]
    - path: "src/components/team/TeamGrid.tsx"
      provides: "Grid wrapper for team member cards"
      exports: ["TeamGrid"]
    - path: "src/app/team/page.tsx"
      provides: "Team page with Board and Staff sections"
      contains: "generateMetadata"
    - path: "src/app/testimonials/page.tsx"
      provides: "Testimonials page with all 4 audience segments"
      contains: "generateMetadata"
  key_links:
    - from: "src/app/team/page.tsx"
      to: "src/data/team-members.ts"
      via: "imports boardMembers and staffMembers"
      pattern: "import.*boardMembers.*staffMembers.*team-members"
    - from: "src/app/team/page.tsx"
      to: "src/components/team/TeamGrid.tsx"
      via: "renders TeamGrid with member data"
      pattern: "import.*TeamGrid"
    - from: "src/app/testimonials/page.tsx"
      to: "src/data/testimonials.ts"
      via: "imports testimonials array"
      pattern: "import.*testimonials"
---

<objective>
Build the Team page (Board of Directors + Staff with card-based layout) and the Testimonials page (quotes from all 4 audience segments with visual distinction).

Purpose: These are the people-focused community pages. The Team page builds trust through named people with rural connections. The Testimonials page provides social proof through peer voices across all audience segments.

Output: TeamMemberCard and TeamGrid components, plus complete Team and Testimonials pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@RESEARCH.md (sections 2.1, 2.2, 5.5, 7.2)
@.planning/phases/06-community-pages/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TeamMemberCard and TeamGrid components, then build the Team page</name>
  <files>
    src/components/team/TeamMemberCard.tsx
    src/components/team/TeamGrid.tsx
    src/app/team/page.tsx
  </files>
  <action>
**TeamMemberCard (src/components/team/TeamMemberCard.tsx):**

A server component (no "use client"). Import `TeamMember` from `@/types`.

Props: `{ member: TeamMember }`

Structure:
```
<article> wrapper with card styling:
  - `bg-white rounded-lg overflow-hidden shadow-sm border border-stone-200`
  - Hover effect: `hover:shadow-md hover:border-accent-300 transition-all duration-300 ease-premium`
  - Gold top border that grows on hover: use a `::before` pseudo via Tailwind `relative before:absolute before:top-0 before:left-0 before:w-full before:h-[2px] before:bg-accent-500 before:scale-x-0 before:origin-left before:transition-transform before:duration-400 hover:before:scale-x-100`

  Avatar placeholder area:
  - `div` with `bg-stone-200 h-48 flex items-center justify-center`
  - Inside: initials of the person (first letter of first name + first letter of last name) in `text-3xl font-heading font-semibold text-stone-400`
  - If `member.imageUrl` exists, use next/image instead (for future use)

  Content area with `p-6 space-y-3`:
  - Name: `<h3>` in `font-heading text-xl font-semibold text-primary-900`
  - Role: `<p>` in `text-sm font-medium text-accent-600 uppercase tracking-wide font-body`
  - Bio: `<p>` in `text-sm text-text-body leading-relaxed`
  - Connection/responsibility: `<p>` with a small icon or marker in `text-xs text-text-muted italic border-t border-stone-100 pt-3 mt-3`
    - For board members (group === "board"): prefix with "Rural connection: "
    - For staff (group === "staff"): prefix with "Responsibility: "
</article>
```

The card must be accessible: use `<article>` semantics, ensure headings are proper `<h3>`.

**TeamGrid (src/components/team/TeamGrid.tsx):**

A server component. Import `TeamMember` from `@/types` and `TeamMemberCard`.

Props: `{ members: TeamMember[], columns?: 2 | 3 | 4 }`

Renders a responsive grid:
- `grid gap-8`
- Default: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` (3-column)
- If `columns` is 4: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`
- Maps over `members` and renders `<TeamMemberCard key={member.id} member={member} />`

**Team page (src/app/team/page.tsx):**

React Server Component. Import boardMembers and staffMembers from `@/data/team-members`, TeamGrid from `@/components/team/TeamGrid`.

**SEO metadata:**
- title: "Our Team | St Katharine Rural Connect"
- description: "Meet the Board of Directors and staff team behind St Katharine Rural Connect. Our team brings rural experience, community commitment, and professional expertise to supporting rural Scotland."

**Page structure (3 sections):**

**Section 1 -- Page Header (stone-50):**
- Overline: "OUR TEAM" in standard overline styling
- H1: "The People Behind SKRC" in `font-heading text-5xl font-semibold text-text-primary`
- Intro: "Our team brings together people who understand rural life firsthand -- from farming families to offshore workers, from community builders to mental health professionals. Every person at SKRC shares a commitment to making rural communities stronger." in `text-lg text-text-body max-w-prose`

**Section 2 -- Board of Directors (stone-100):** [TEAM-01]
- H2: "Board of Directors" in `font-heading text-4xl font-semibold text-text-primary`
- Subtitle: "Our Board provides strategic leadership, governance, and stewardship, ensuring SKRC stays true to its mission and the communities it serves." in `text-base text-text-muted max-w-prose mb-8`
- `<TeamGrid members={boardMembers} columns={4} />` (4 columns for 4 board members)

**Section 3 -- Staff Team (stone-50):** [TEAM-02]
- H2: "Our Staff" in `font-heading text-4xl font-semibold text-text-primary`
- Subtitle: "The team who deliver SKRC's services day to day, bringing warmth, expertise, and genuine care to every interaction." in `text-base text-text-muted max-w-prose mb-8`
- `<TeamGrid members={staffMembers} columns={3} />` (3 columns for 3 staff)

TEAM-03 is satisfied by using TeamMemberCard consistently across both sections with identical card styling.
  </action>
  <verify>
- `grep "export" src/components/team/TeamMemberCard.tsx` confirms export
- `grep "export" src/components/team/TeamGrid.tsx` confirms export
- `grep "boardMembers" src/app/team/page.tsx` confirms import
- `grep "staffMembers" src/app/team/page.tsx` confirms import
- `grep "TeamGrid" src/app/team/page.tsx` confirms component usage
- `grep "generateMetadata\|export const metadata" src/app/team/page.tsx` confirms SEO
- `grep "<h1\|<h2\|<h3" src/app/team/page.tsx` shows proper hierarchy
- `npm run build` passes
  </verify>
  <done>
- TEAM-01: Board of Directors section with 4 cards showing name, role, bio, and rural connection
- TEAM-02: Staff section with 3 cards showing name, position, bio, and area of responsibility
- TEAM-03: Consistent card-based layout using shared TeamMemberCard component with gold top-border hover, initials placeholder, and connection/responsibility footer
- TeamMemberCard is reusable and handles both board and staff via the `group` discriminator
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the Testimonials page with audience segment distinction</name>
  <files>src/app/testimonials/page.tsx</files>
  <action>
Create `src/app/testimonials/page.tsx` as a React Server Component. Import `testimonials` from `@/data/testimonials`.

**SEO metadata:**
- title: "Testimonials | St Katharine Rural Connect"
- description: "Hear from farmers, offshore workers, families, and community members about how St Katharine Rural Connect has made a difference in their lives."

**Visual distinction per segment (TESTI-03):**
Define a segment color/style map within the component (or as a const at top of file):

```ts
const segmentStyles: Record<AudienceSegment, { label: string; borderColor: string; bgColor: string; iconColor: string }> = {
  "farmer": { label: "Farmer", borderColor: "border-l-green-700", bgColor: "bg-green-50", iconColor: "text-green-700" },
  "offshore-worker": { label: "Offshore Worker", borderColor: "border-l-blue-700", bgColor: "bg-blue-50", iconColor: "text-blue-700" },
  "community": { label: "Community Member", borderColor: "border-l-secondary-500", bgColor: "bg-secondary-100", iconColor: "text-secondary-700" },
  "family": { label: "Family Member", borderColor: "border-l-accent-500", bgColor: "bg-accent-100", iconColor: "text-accent-600" },
};
```

These colors provide immediate visual distinction while remaining accessible. The left border color is the primary differentiator.

**Page structure (3 sections):**

**Section 1 -- Page Header (stone-50):**
- Overline: "TESTIMONIALS" in standard overline styling
- H1: "Voices from Our Community" in `font-heading text-5xl font-semibold text-text-primary`
- Intro: "The people we serve are at the heart of everything we do. Here are some of their stories -- in their own words." in `text-lg text-text-body max-w-prose`

**Section 2 -- Testimonials Grid (stone-100):** [TESTI-01, TESTI-02, TESTI-03]
- Layout: `grid grid-cols-1 lg:grid-cols-2 gap-8`
- Each testimonial rendered as a card/block:

```
<blockquote> wrapper:
  - `rounded-lg p-8 shadow-sm border border-stone-200 ${segmentStyles[testimonial.segment].bgColor}`
  - Left border for segment distinction: `border-l-4 ${segmentStyles[testimonial.segment].borderColor}`

  Large decorative opening quote mark:
  - `<span>` with a large gold quotation mark character (") or the unicode left double quotation mark
  - Styled: `text-5xl font-heading text-accent-500 leading-none block mb-2` (gold, large, decorative)

  Quote text:
  - `<p>` in `text-base text-text-body leading-relaxed italic`
  - Use the full quote from the data file

  Attribution (TESTI-02):
  - `<footer>` element (semantic HTML for blockquote attribution)
  - `<cite>` containing: "-- {firstName}, {identifier}"
  - Styled: `text-sm font-medium text-text-primary mt-4 block not-italic`
  - The em dash should be the unicode character

  Segment badge:
  - Small pill/badge showing the audience segment label
  - `<span>` with `inline-block px-3 py-1 rounded-full text-xs font-medium ${segmentStyles[testimonial.segment].iconColor} ${segmentStyles[testimonial.segment].bgColor} border border-current/20 mt-3`
  - Text: the `label` from segmentStyles (e.g., "Farmer", "Offshore Worker")
</blockquote>
```

**Section 3 -- CTA Section (primary-900 dark background):**
- H2: "Your Story Matters Too" in `font-heading text-4xl font-semibold text-text-on-dark text-center`
- Paragraph: "Whether you're a farmer, offshore worker, family member, or community member, we're here for you. Every conversation is confidential, and there's never any pressure." in `text-lg text-stone-300 text-center max-w-prose mx-auto`
- CTA button: "Get in Touch" linking to `/contact`
  - Styled: `inline-block mt-8 px-8 py-4 bg-accent-500 text-primary-950 font-body font-semibold rounded-md hover:bg-accent-400 transition-colors duration-300`

**Accessibility notes:**
- Use `<blockquote>` for testimonial quotes (semantic)
- Use `<cite>` inside `<footer>` for attribution (semantic)
- Ensure color distinction is not the ONLY differentiator -- the segment badge provides text-based distinction too
- Large quote mark is decorative, use `aria-hidden="true"`
  </action>
  <verify>
- `grep "testimonials" src/app/testimonials/page.tsx` confirms data import
- `grep "blockquote" src/app/testimonials/page.tsx` confirms semantic quote markup
- `grep "cite" src/app/testimonials/page.tsx` confirms attribution markup
- `grep "firstName\|identifier" src/app/testimonials/page.tsx` confirms TESTI-02
- `grep "farmer.*offshore\|segmentStyles\|border-l-" src/app/testimonials/page.tsx` confirms visual distinction (TESTI-03)
- `grep "generateMetadata\|export const metadata" src/app/testimonials/page.tsx` confirms SEO
- `npm run build` passes
  </verify>
  <done>
- TESTI-01: All 4 audience segment testimonials rendered on a single page
- TESTI-02: Each testimonial shows first name and identifier in a cite element (e.g., "-- James, dairy farmer near Huntly")
- TESTI-03: Visual distinction via colored left border per segment (green=farmer, blue=offshore, purple=community, gold=family), tinted background, and labeled segment badge
- Large decorative gold quote marks add premium feel
- CTA section on dark background invites visitors to reach out
  </done>
</task>

</tasks>

<verification>
1. `ls src/components/team/{TeamMemberCard,TeamGrid}.tsx src/app/team/page.tsx src/app/testimonials/page.tsx` -- all 4 files exist
2. Visit `/team` -- Board (4 cards) and Staff (3 cards) render with consistent card design
3. Visit `/testimonials` -- 4 testimonials render with distinct colored left borders and segment badges
4. Each testimonial shows the quote, first name, and identifier
5. Team cards show gold top-border on hover
6. Both pages have proper heading hierarchy and SEO metadata
7. `npm run build` passes
</verification>

<success_criteria>
- Team page shows Board of Directors (4 members, 4-column grid) and Staff (3 members, 3-column grid)
- Each team card displays name, role, bio, and connection/responsibility with consistent styling
- Cards have hover effects: shadow increase, border color change, gold top-border animation
- Testimonials page displays all 4 audience segment quotes
- Each testimonial has first name, identifier, decorative gold quote mark, and segment badge
- Visual distinction between segments via colored left border and tinted background
- Both pages responsive (mobile stacks to single column)
</success_criteria>

<output>
After completion, create `.planning/phases/06-community-pages/06-03-SUMMARY.md`
</output>
