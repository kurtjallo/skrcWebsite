---
phase: 06-community-pages
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/get-involved/page.tsx
  - src/app/partnerships/page.tsx
autonomous: true

must_haves:
  truths:
    - "Get Involved page has sections for Volunteer, Attend Events, Donate, Partner, and Share"
    - "Each involvement option has a clear CTA routing to the appropriate page or action"
    - "Partnerships page describes the partnership approach"
    - "Partnerships page displays partner logos (with text fallback for missing images)"
    - "Partnerships page has a 'Become a Partner' CTA"
  artifacts:
    - path: "src/app/get-involved/page.tsx"
      provides: "Get Involved page with 5 involvement sections and CTAs"
      contains: "generateMetadata"
    - path: "src/app/partnerships/page.tsx"
      provides: "Partnerships page with approach, logos, and CTA"
      contains: "generateMetadata"
  key_links:
    - from: "src/app/get-involved/page.tsx"
      to: "/events"
      via: "Attend Events CTA links to events page"
      pattern: "href.*events"
    - from: "src/app/get-involved/page.tsx"
      to: "/partnerships"
      via: "Partner CTA links to partnerships page"
      pattern: "href.*partnerships"
    - from: "src/app/get-involved/page.tsx"
      to: "/contact"
      via: "Volunteer CTA links to contact page"
      pattern: "href.*contact"
    - from: "src/app/partnerships/page.tsx"
      to: "src/data/partners.ts"
      via: "imports partners and partnershipApproach"
      pattern: "import.*partners"
    - from: "src/app/partnerships/page.tsx"
      to: "/contact"
      via: "Become a Partner CTA links to contact"
      pattern: "href.*contact"
---

<objective>
Build the Get Involved page (5 ways to participate with CTAs) and the Partnerships page (approach description, partner logos, and "Become a Partner" CTA).

Purpose: These are the action-oriented community pages. Get Involved converts interested visitors into active participants. Partnerships builds credibility through partner logos and invites new organisational relationships.

Output: Complete Get Involved page at `/get-involved` and Partnerships page at `/partnerships`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@RESEARCH.md (sections 2.1, 2.2, 4.3 CTA hierarchy, 5.1 voice/tone)
@.planning/phases/06-community-pages/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Get Involved page with 5 involvement sections and CTAs</name>
  <files>src/app/get-involved/page.tsx</files>
  <action>
Create `src/app/get-involved/page.tsx` as a React Server Component.

Import lucide-react icons: `Heart, Calendar, Gift, Handshake, Share2` (one per involvement type).

**SEO metadata:**
- title: "Get Involved | St Katharine Rural Connect"
- description: "Join St Katharine Rural Connect as a volunteer, event attendee, donor, partner, or advocate. There are many ways to strengthen rural communities."

**Define involvement options inline** (no data file needed -- this content is structural, not data-driven):

```ts
const involvementOptions = [
  {
    icon: Heart,
    title: "Volunteer With Us",
    description: "Give your time and skills to support rural communities. Whether you can spare an hour a week or a day a month, there's a role that fits. Volunteers are the backbone of our community work -- from helping at events to providing peer support.",
    ctaText: "Register Your Interest",
    ctaHref: "/contact",
  },
  {
    icon: Calendar,
    title: "Attend Events",
    description: "Join us for workshops, gatherings, and community events across rural Scotland. Our events bring people together for learning, connection, and support. Everyone is welcome, whether you're looking for help or simply want to be part of something positive.",
    ctaText: "See Upcoming Events",
    ctaHref: "/events",
  },
  {
    icon: Gift,
    title: "Donate Today",
    description: "Your generosity helps us reach more people in rural communities who need support. Every contribution, no matter the size, goes directly towards delivering services, running events, and keeping our support accessible and free for those who need it most.",
    ctaText: "Make a Donation",
    ctaHref: "/contact",
  },
  {
    icon: Handshake,
    title: "Become a Partner",
    description: "We work alongside community organisations, agricultural networks, mental health providers, local agencies, and businesses committed to rural wellbeing. Together, we can create a stronger network of support across rural Scotland.",
    ctaText: "Explore Partnership",
    ctaHref: "/partnerships",
  },
  {
    icon: Share2,
    title: "Share Our Story",
    description: "Sometimes the most powerful thing you can do is spread the word. Share our work with your community, on social media, or simply by telling a neighbour about us. You never know who might need to hear that support is available.",
    ctaText: "Learn More",
    ctaHref: "/about",
  },
];
```

**Page structure (3 sections):**

**Section 1 -- Page Header (stone-50):**
- Overline: "GET INVOLVED" in standard overline styling
- H1: "Be Part of Something Meaningful" in `font-heading text-5xl font-semibold text-text-primary`
- Intro: "There are many ways to support rural communities through St Katharine Rural Connect. Whether you have time, resources, connections, or simply a willingness to care, there's a place for you." in `text-lg text-text-body max-w-prose`

**Section 2 -- Involvement Options (stone-100):** [INVOLVE-01, INVOLVE-02]
- Layout: Each option as a full-width card, stacked vertically with `space-y-8`
- Each card uses a two-column layout on desktop: `grid grid-cols-1 md:grid-cols-[auto_1fr] gap-8 items-start`

Each involvement card structure:
```
<div> card wrapper:
  - `bg-white rounded-lg p-8 shadow-sm border border-stone-200 hover:shadow-md transition-shadow duration-300`

  Left column (icon area):
  - `flex-shrink-0`
  - Icon wrapper: `w-16 h-16 rounded-full bg-accent-100 flex items-center justify-center`
  - Icon: rendered lucide icon at size 28, `text-accent-600`

  Right column (content):
  - H2: `font-heading text-3xl font-semibold text-primary-900`
  - Description: `text-base text-text-body leading-relaxed mt-3`
  - CTA link: `<a>` styled as inline-block button:
    `mt-6 inline-block px-6 py-3 bg-primary-900 text-text-on-dark font-body font-medium rounded-md hover:bg-primary-700 transition-colors duration-300`
    - Text: the `ctaText` from the option
    - href: the `ctaHref` from the option
```

**Section 3 -- CTA Banner (primary-900 dark background):**
- H2: "Not Sure Where to Start?" in `font-heading text-4xl font-semibold text-text-on-dark text-center`
- Paragraph: "That's perfectly fine. Get in touch and we'll help you find the right way to get involved. No commitment, no pressure -- just a conversation." in `text-lg text-stone-300 text-center max-w-prose mx-auto`
- CTA button: "Contact Our Team" linking to `/contact`
  - `inline-block mt-8 px-8 py-4 bg-accent-500 text-primary-950 font-body font-semibold rounded-md hover:bg-accent-400 transition-colors duration-300`

**Accessibility:**
- Each involvement section heading is `<h2>` (proper hierarchy under the page `<h1>`)
- CTA links have descriptive text (not just "Click here")
- Icons are decorative, add `aria-hidden="true"` to each
  </action>
  <verify>
- `grep "Volunteer\|Attend Events\|Donate\|Partner\|Share" src/app/get-involved/page.tsx` confirms all 5 sections
- `grep "href.*contact\|href.*events\|href.*partnerships\|href.*about" src/app/get-involved/page.tsx` confirms CTAs route correctly
- `grep "generateMetadata\|export const metadata" src/app/get-involved/page.tsx` confirms SEO
- `grep "aria-hidden" src/app/get-involved/page.tsx` confirms icon accessibility
- `npm run build` passes
  </verify>
  <done>
- INVOLVE-01: All 5 involvement sections present (Volunteer, Attend Events, Donate, Partner, Share) with icon, description, and CTA
- INVOLVE-02: Each CTA routes to the appropriate page (/contact for Volunteer and Donate, /events for Attend, /partnerships for Partner, /about for Share)
- Cards have two-column layout with icon circle and hover effects
- Dark CTA banner at bottom for visitors who are unsure
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the Partnerships page with approach, logos, and CTA</name>
  <files>src/app/partnerships/page.tsx</files>
  <action>
Create `src/app/partnerships/page.tsx` as a React Server Component. Import `partners` and `partnershipApproach` from `@/data/partners`.

**SEO metadata:**
- title: "Partnerships | St Katharine Rural Connect"
- description: "Learn about St Katharine Rural Connect's partnership approach and meet the organisations working alongside us to strengthen rural communities."

**Page structure (4 sections):**

**Section 1 -- Page Header (stone-50):**
- Overline: "PARTNERSHIPS" in standard overline styling
- H1: "Stronger Together" in `font-heading text-5xl font-semibold text-text-primary`
- Intro: "Real, lasting change in rural communities happens through collaboration. We work alongside organisations that share our commitment to supporting rural people and places." in `text-lg text-text-body max-w-prose`

**Section 2 -- Partnership Approach (stone-100):** [PART-01]
- Two-column layout on desktop: `grid grid-cols-1 lg:grid-cols-2 gap-12 items-start`
- Left column:
  - H2: "Our Approach to Partnership" in `font-heading text-4xl font-semibold text-text-primary`
  - Narrative paragraph: "We believe that the best support for rural communities comes from organisations working together, not in isolation. Our partnerships are built on shared values, mutual respect, and a genuine commitment to making rural life better. We bring local knowledge and community trust; our partners bring specialist expertise and resources. Together, we create something neither could achieve alone." in `text-base text-text-body leading-relaxed`

- Right column:
  - H3: "We partner with:" in `font-heading text-xl font-semibold text-primary-900 mb-4`
  - Render `partnershipApproach` array as a styled list:
    - `<ul>` with `space-y-4`
    - Each item: `<li>` with a gold accent marker (small gold diamond or bullet)
      - Use a `<span>` with `inline-block w-2 h-2 bg-accent-500 rotate-45 mr-3 flex-shrink-0` for a diamond bullet
      - Text in `text-base text-text-body`
    - Layout each li: `flex items-start`

**Section 3 -- Partner Logos (stone-50):** [PART-02]
- H2: "Our Partners" in `font-heading text-4xl font-semibold text-text-primary text-center`
- Subtitle: "We are proud to work alongside these organisations." in `text-base text-text-muted text-center mb-12`

- Partner logo grid: `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-8 items-center`
- Each partner rendered as:
  ```
  <div> wrapper:
    - `bg-white rounded-lg p-6 flex flex-col items-center justify-center border border-stone-200 hover:border-accent-300 hover:shadow-md transition-all duration-300 min-h-[120px]`

    Logo area:
    - Try to render partner logo using next/image:
      `<Image src={partner.logoUrl} alt={partner.name} width={120} height={60} className="grayscale hover:grayscale-0 transition-all duration-500 object-contain" />`
    - Since logos don't actually exist yet (placeholder SVGs), wrap in a try-catch pattern OR use a conditional:
      Show the partner name as text fallback when image is not available:
      `<span className="font-body font-medium text-sm text-text-muted text-center">{partner.name}</span>`
    - BEST APPROACH: Since images won't exist yet, render the partner name text directly with a subtle icon placeholder. Do NOT use next/image with non-existent files (build will fail). Instead:
      `<div className="text-center">
        <div className="w-16 h-16 bg-stone-100 rounded-full flex items-center justify-center mx-auto mb-3">
          <span className="text-lg font-heading font-semibold text-stone-400">{partner.name.charAt(0)}</span>
        </div>
        <span className="font-body font-medium text-sm text-text-muted">{partner.name}</span>
      </div>`
    - Add grayscale styling to the placeholder circles to mimic the intended logo treatment

    If partner.url exists, wrap in `<a href={partner.url} target="_blank" rel="noopener noreferrer">`
  ```

- Below the grid, subtle note: "Partner logos will be displayed here. Showing partner names as placeholders." in `text-xs text-text-muted text-center mt-8 italic`

**Section 4 -- Become a Partner CTA (primary-900 dark background):** [PART-03]
- H2: "Become a Partner" in `font-heading text-4xl font-semibold text-text-on-dark text-center`
- Paragraph: "If your organisation shares our commitment to rural wellbeing, we'd love to explore how we can work together. Partnership with SKRC means joining a growing network dedicated to ensuring no rural person faces their challenges alone." in `text-lg text-stone-300 text-center max-w-prose mx-auto`
- CTA button: "Get in Touch About Partnership" linking to `/contact`
  - `inline-block mt-8 px-8 py-4 bg-accent-500 text-primary-950 font-body font-semibold rounded-md hover:bg-accent-400 transition-colors duration-300`
- Below the button, a smaller text: "Or call us on 01234 567890" as a `tel:` link in `text-sm text-stone-400 mt-4 block`
  </action>
  <verify>
- `grep "partnershipApproach\|partners" src/app/partnerships/page.tsx` confirms data imports
- `grep "Become a Partner" src/app/partnerships/page.tsx` confirms PART-03 CTA
- `grep "Our Approach\|Our Partners" src/app/partnerships/page.tsx` confirms PART-01 and PART-02 sections
- `grep "href.*contact" src/app/partnerships/page.tsx` confirms CTA routing
- `grep "tel:" src/app/partnerships/page.tsx` confirms phone link
- `grep "generateMetadata\|export const metadata" src/app/partnerships/page.tsx` confirms SEO
- `npm run build` passes
  </verify>
  <done>
- PART-01: Partnership approach description with narrative text and gold diamond-bullet list of partner types
- PART-02: Partner logos grid with text/initial fallback for missing images, grayscale treatment on placeholders, hover effects
- PART-03: "Become a Partner" CTA on dark primary-900 background with link to /contact and phone number
- External partner links open in new tab with proper rel attributes
  </done>
</task>

</tasks>

<verification>
1. `ls src/app/get-involved/page.tsx src/app/partnerships/page.tsx` -- both files exist
2. Visit `/get-involved` -- 5 involvement sections with icons and CTAs render correctly
3. Visit `/partnerships` -- approach description, partner grid, and "Become a Partner" CTA render
4. CTAs on Get Involved page link to correct destinations (/contact, /events, /partnerships, /about)
5. Partnerships page imports data from partners.ts
6. Both pages have alternating section backgrounds and dark CTA sections
7. `npm run build` passes
</verification>

<success_criteria>
- Get Involved page has all 5 involvement options (Volunteer, Attend Events, Donate, Partner, Share) with icons and working CTAs
- Each CTA routes to the correct page (no dead links)
- Partnerships page describes the partnership approach with narrative text and partner type list
- Partner logos area shows placeholder names/initials (graceful degradation for missing images)
- "Become a Partner" CTA links to /contact with phone number alternative
- Both pages are responsive, accessible, and follow the design system
</success_criteria>

<output>
After completion, create `.planning/phases/06-community-pages/06-04-SUMMARY.md`
</output>
