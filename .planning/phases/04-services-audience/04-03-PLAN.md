---
phase: 04-services-audience
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/who-we-serve/page.tsx
  - src/components/services/AudienceCard.tsx
autonomous: true

must_haves:
  truths:
    - "Who We Serve page displays all four audience segments"
    - "Each audience segment shows relevant services linked to pillar detail pages"
    - "Each audience segment lists specific access points (phone, email, forms, workshops)"
    - "Audience segments include testimonials from people in that group"
    - "Page uses warm, person-first language throughout"
  artifacts:
    - path: "src/app/who-we-serve/page.tsx"
      provides: "Who We Serve page"
      exports: ["default", "metadata"]
    - path: "src/components/services/AudienceCard.tsx"
      provides: "Audience segment card component"
      exports: ["AudienceCard"]
  key_links:
    - from: "src/app/who-we-serve/page.tsx"
      to: "src/data/audiences.ts"
      via: "import audienceSegments"
      pattern: "import.*audienceSegments.*from.*data/audiences"
    - from: "src/app/who-we-serve/page.tsx"
      to: "src/data/services.ts"
      via: "import servicePillars for linking"
      pattern: "import.*servicePillars.*from.*data/services"
    - from: "src/components/services/AudienceCard.tsx"
      to: "/services/[slug]"
      via: "links to relevant pillar detail pages"
      pattern: "href.*services/"
---

<objective>
Create the Who We Serve page with audience segment cards, relevant service links, and access points.

Purpose: Delivers AUD-01 (Who We Serve overview), AUD-02 (audience-specific content sections with relevant services, testimonials, contact options), and AUD-03 (access points listed per audience). This page serves as the destination for the homepage "I am a..." self-identification cards, routing each audience to their relevant support pathways.

Output: Who We Serve page at /who-we-serve with four audience segment sections, each showing relevant services and access points.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@RESEARCH.md
@.planning/phases/04-services-audience/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AudienceCard component and Who We Serve page</name>
  <files>
    src/components/services/AudienceCard.tsx
    src/app/who-we-serve/page.tsx
  </files>
  <action>
    **src/components/services/AudienceCard.tsx** -- Create a component for displaying an audience segment section.

    Props:
    ```typescript
    interface AudienceCardProps {
      segment: AudienceSegment;
      relevantPillars: ServicePillar[];  // pre-filtered pillars matching this audience
      reversed?: boolean;                // alternate layout direction for visual rhythm
    }
    ```

    Layout structure (each audience gets a full-width section, not a small card):

    - Two-column layout on desktop (md:grid-cols-2, gap-12), stacked on mobile
    - `reversed` prop swaps column order (text left/right alternates) using md:order-1/md:order-2
    - **Column A (content):**
      - Audience name as h2 in font-heading, text-4xl, font-semibold
      - Gold accent line below heading (2px, w-16, bg-accent-500, my-4)
      - Description paragraph in text-lg, text-body, leading-relaxed
      - **"Services for you" sub-section:**
        - Subheading: "How we can help" in font-heading, text-xl, mt-8
        - List of relevant pillar links: each as a Link to /services/{pillar.slug}, styled as inline items with accent-600 text, hover:accent-500, with right arrow icon
        - Display pillar.name (plain language name)
      - **"Get in touch" CTA:** Button linking to /contact, styled same as other CTAs across the site

    - **Column B (access points + testimonial):**
      - **Access points card:** Stone-200 background, rounded-lg, p-6
        - Heading: "Ways to reach us" in font-heading, text-xl
        - List of access points: each with a check icon (lucide Check or CheckCircle in accent-500) + method in font-semibold + description in text-body
        - Spacing: space-y-4
      - **Testimonial (if exists):** Below the access points card, mt-6
        - Quote in font-heading, text-lg, italic, text-text-body
        - Attribution: "-- {name}, {identifier}" in text-muted, text-sm
        - Left border: border-l-4 border-accent-500, pl-6

    Alternating backgrounds: Use the `reversed` prop to alternate which side content appears on, plus alternate the section background between stone-50 and stone-100.

    **src/app/who-we-serve/page.tsx** -- Create the Who We Serve page.

    This is a React Server Component.

    Structure:

    1. **Hero section** (stone-50 background, py-16 md:py-24):
       - Overline: "WHO WE SERVE" in uppercase, tracking-widest, text-xs, font-bold, text-accent-600
       - Heading: "We're Here for You" in font-heading, text-display, font-light
       - Subtitle: "Whether you're farming the land, working offshore, supporting family from home, or looking to connect with your community -- there's a place for you here." in text-lg, text-body, max-w-2xl mx-auto, text-center
       - Centered layout

    2. **Audience segments** (alternating backgrounds):
       - Import audienceSegments from data/audiences
       - Import servicePillars from data/services
       - For each audience segment:
         - Filter servicePillars to find pillars where pillar.id matches the segment's relevantPillarIds
         - Render AudienceCard with segment, relevant pillars, and reversed={index is odd}
         - Use SectionWrapper with alternating backgrounds if available
         - Each segment gets full viewport-width section treatment (not small cards)

    3. **"Not sure where you fit?" section** (primary-900 dark background, py-16):
       - Heading: "Not sure where you fit?" in font-heading, text-3xl, text-on-dark, text-center
       - Body: "That's absolutely fine. Our support isn't about fitting into a category -- it's about getting you the help that's right for you. Get in touch and we'll take it from there." in text-lg, text-on-dark/80, text-center, max-w-2xl mx-auto
       - Dual CTAs centered:
         - Primary: "Get in Touch" button (accent-500 bg, primary-950 text, hover effect)
         - Secondary: "View Our Services" text link (text-accent-300, hover:text-accent-200, underline) linking to /services

    Export metadata:
    ```typescript
    export const metadata = {
      title: "Who We Serve | St Katharine Rural Connect",
      description: "Support for farmers, offshore workers, families, and community members. Find out how St Katharine Rural Connect serves your community.",
    };
    ```

    Design notes:
    - Alternating section backgrounds create visual rhythm (stone-50, stone-100, stone-50, stone-100)
    - Two-column layouts alternate direction for visual interest
    - Person-first language throughout: "Farmers and agricultural workers" not "Agricultural clients"
    - Access points are specific and actionable, not generic
    - The page should feel like a warm invitation, not a service catalogue
    - Proper heading hierarchy: h1 for page title, h2 for each audience segment, h3 for sub-sections within segments
    - Use semantic HTML: sections, articles where appropriate
    - Ensure responsive: all two-column layouts stack on mobile
  </action>
  <verify>
    Run `npm run dev` and navigate to `/who-we-serve`. Verify:
    1. Page renders with "We're Here for You" heading
    2. All four audience segments display: Farmers, Offshore Workers, Families, Community
    3. Each segment shows its description, relevant service pillar links, access points, and testimonial
    4. Service pillar links point to correct /services/{slug} URLs
    5. Two-column layouts alternate direction (first segment text-left/access-right, second reversed)
    6. Access points list specific methods with descriptions
    7. Testimonials render with attribution
    8. "Not sure where you fit?" dark section appears at bottom with dual CTAs
    9. Page is responsive (stacks to single column on mobile)
    10. Page title is "Who We Serve | St Katharine Rural Connect"

    Run `npx tsc --noEmit` for type safety.
  </verify>
  <done>
    Who We Serve page at /who-we-serve displays all four audience segments with relevant service links, access points per audience, testimonials, alternating two-column layouts, and reassuring CTA section. All links to service pillar pages are correct. Page is responsive and uses person-first language throughout.
  </done>
</task>

</tasks>

<verification>
- `/who-we-serve` renders all four audience segments
- Each segment links to the correct service pillar detail pages
- Access points are listed per audience with specific methods
- Testimonials appear for each segment
- Two-column layouts alternate direction and are responsive
- Dark CTA section at bottom with dual calls to action
- Page metadata is correct
- `npx tsc --noEmit` passes
- No broken links to /services/* pages
</verification>

<success_criteria>
AUD-01 satisfied: Who We Serve overview page with all audience segments
AUD-02 satisfied: Audience-specific content sections with relevant services, testimonials, and contact options
AUD-03 satisfied: Access points listed per audience (phone, email, forms, workshops, virtual contact, flexible scheduling)
</success_criteria>

<output>
After completion, create `.planning/phases/04-services-audience/04-03-SUMMARY.md`
</output>
