---
phase: 04-services-audience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/services.ts
  - src/types/audience.ts
  - src/data/services.ts
  - src/data/audiences.ts
  - src/components/services/PillarCard.tsx
  - src/components/services/WhatHappensWhenYouCall.tsx
  - src/app/services/page.tsx
autonomous: true

must_haves:
  truths:
    - "Services overview page displays all four pillars with user-centered plain language names"
    - "Each pillar card links to its individual detail page via correct slug"
    - "Pillar cards show hover effects with gold accent lines per design system"
    - "Service data uses plain language naming throughout (e.g. 'Supporting Farm Life' not 'Agricultural Support')"
  artifacts:
    - path: "src/types/services.ts"
      provides: "ServicePillar type definition"
      exports: ["ServicePillar"]
    - path: "src/types/audience.ts"
      provides: "AudienceSegment type definition"
      exports: ["AudienceSegment", "AccessPoint"]
    - path: "src/data/services.ts"
      provides: "All four service pillar data objects"
      exports: ["servicePillars", "getServiceBySlug"]
    - path: "src/data/audiences.ts"
      provides: "All four audience segment data objects"
      exports: ["audienceSegments"]
    - path: "src/components/services/PillarCard.tsx"
      provides: "Reusable pillar card component with hover effects"
      exports: ["PillarCard"]
    - path: "src/components/services/WhatHappensWhenYouCall.tsx"
      provides: "Reassurance section component"
      exports: ["WhatHappensWhenYouCall"]
    - path: "src/app/services/page.tsx"
      provides: "Services overview page"
  key_links:
    - from: "src/app/services/page.tsx"
      to: "src/data/services.ts"
      via: "import servicePillars"
      pattern: "import.*servicePillars.*from.*data/services"
    - from: "src/app/services/page.tsx"
      to: "src/components/services/PillarCard.tsx"
      via: "renders PillarCard for each pillar"
      pattern: "servicePillars\\.map.*PillarCard"
    - from: "src/components/services/PillarCard.tsx"
      to: "/services/[slug]"
      via: "Next.js Link to pillar detail page"
      pattern: "href.*services/.*slug"
---

<objective>
Create the data layer, TypeScript types, reusable service components, and Services overview page for Phase 4.

Purpose: Establishes the data foundation that both the pillar detail pages (Plan 02) and Who We Serve page (Plan 03) depend on, plus delivers the Services overview page (SERV-01) with pillar cards (SERV-06) and the reusable WhatHappensWhenYouCall component (SERV-07).

Output: TypeScript types and data files for services and audiences, PillarCard and WhatHappensWhenYouCall components, and the /services overview page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript types and data files for services and audiences</name>
  <files>
    src/types/services.ts
    src/types/audience.ts
    src/data/services.ts
    src/data/audiences.ts
  </files>
  <action>
    **src/types/services.ts** -- Define the ServicePillar type:
    ```typescript
    export interface ServicePillar {
      id: string;                    // e.g. "wellbeing"
      slug: string;                  // URL slug e.g. "supporting-your-wellbeing"
      name: string;                  // User-facing plain language name
      orgName: string;               // Organizational/internal name
      tagline: string;               // One-line description for cards
      whoItsFor: string;             // "Who it's for" -- one sentence about the person, not the problem
      whatHappens: string;           // "What happens" -- 2-3 sentences about the experience
      howToGetStarted: string;       // "How to get started" -- single clear action step
      services: string[];            // List of specific service offerings
      icon: string;                  // Lucide icon name (e.g. "Heart", "Wheat", "Anchor", "Users")
      testimonial?: {
        quote: string;
        name: string;
        identifier: string;          // e.g. "dairy farmer near Huntly"
      };
    }
    ```

    **src/types/audience.ts** -- Define audience types:
    ```typescript
    export interface AccessPoint {
      method: string;                // e.g. "Phone/email"
      description: string;           // e.g. "Call or email us directly"
    }

    export interface AudienceSegment {
      id: string;                    // e.g. "farmers"
      name: string;                  // e.g. "Farmers and Agricultural Workers"
      shortName: string;             // e.g. "Farmers" (for cards/nav)
      description: string;           // 2-3 sentences about how SKRC serves them
      relevantPillarIds: string[];   // IDs of relevant service pillars
      accessPoints: AccessPoint[];
      testimonial?: {
        quote: string;
        name: string;
        identifier: string;
      };
    }
    ```

    **src/data/services.ts** -- Create all four service pillar data objects. Export `servicePillars` array and `getServiceBySlug(slug: string)` helper function. Use the EXACT plain language names from the spec:

    1. **Supporting Your Wellbeing** (slug: "supporting-your-wellbeing", icon: "Heart")
       - whoItsFor: "For anyone in our rural community who could use someone to talk to, or a hand finding the right support."
       - whatHappens: "Whether you're dealing with stress, grief, loneliness, or just a difficult season, we're here to listen. We can connect you with local services, offer peer support sessions, or simply provide a safe space to talk things through. No referral needed."
       - howToGetStarted: "Give us a ring or fill in our contact form -- we'll take it from there."
       - services: ["Individual support and referrals", "Mental health and emotional wellness resources", "Peer support and workshops (stress, burnout, grief, resilience)", "Programs addressing rural isolation"]

    2. **Supporting Farm Life** (slug: "supporting-farm-life", icon: "Wheat")
       - whoItsFor: "For farming families navigating the pressures, uncertainties, and rewards of life on the land."
       - whatHappens: "Farming is more than a job -- it's a way of life, and it comes with unique pressures. We offer practical support sessions, help you find grants and financial assistance, and create spaces where farming folk can connect and share what they're going through."
       - howToGetStarted: "Get in touch by phone or online -- no forms, no fuss."
       - services: ["Farm wellbeing and sustainability sessions", "Financial, operational, and social support referrals", "Farm-focused mental wellness", "Access to grants, programmes, and assistance"]

    3. **Supporting Those Who Work Away** (slug: "supporting-those-who-work-away", icon: "Anchor")
       - whoItsFor: "For offshore, rotational, and remote workers -- and the families who hold things together back home."
       - whatHappens: "Working away can leave you feeling disconnected from everything that matters. We offer remote check-ins, peer connection for isolated workers, and support for families managing separation. Whether you're on a rig, at sea, or on rotation, we're here."
       - howToGetStarted: "Reach out by phone, email, or online -- we work around your schedule."
       - services: ["Remote-access support and check-ins", "Peer connection for isolated workers", "Family support during separation", "Reintegration support"]

    4. **Strengthening Our Community** (slug: "strengthening-our-community", icon: "Users")
       - whoItsFor: "For anyone who wants to be part of building a stronger, more connected rural community."
       - whatHappens: "Community doesn't just happen -- it takes people showing up. We bring neighbours together through events, workshops, and volunteering. Whether you want to learn something new, share a skill, or just meet folk nearby, there's a place for you here."
       - howToGetStarted: "Check our events calendar or get in touch to find out what's happening near you."
       - services: ["Local events and gatherings", "Volunteer engagement", "Workshops and skill-sharing", "Intergenerational programmes", "Partnerships with local groups"]

    Include testimonials from CLAUDE.md:
    - Wellbeing: use the Farmer testimonial (James, dairy farmer near Huntly)
    - Farm Life: use the Family Member testimonial (Sarah, farmer's wife)
    - Work Away: use the Offshore Worker testimonial (Craig, offshore worker)
    - Community: use the Community Member testimonial (Margaret, community member)

    **src/data/audiences.ts** -- Create all four audience segment data objects. Export `audienceSegments` array:

    1. **Farmers and Agricultural Workers** (id: "farmers", shortName: "Farmers")
       - relevantPillarIds: ["wellbeing", "farm-life"]
       - accessPoints: [{method: "Phone or email", description: "Call or email us directly"}, {method: "Online form", description: "Fill in our contact form"}, {method: "Partner referrals", description: "Referred through one of our partner organisations"}, {method: "Farm workshops", description: "Drop in at one of our farm-focused sessions"}]

    2. **Offshore, Rotational, and Remote Workers** (id: "offshore-workers", shortName: "Offshore Workers")
       - relevantPillarIds: ["wellbeing", "work-away"]
       - accessPoints: [{method: "Remote or virtual contact", description: "Phone, video call, or online chat"}, {method: "Flexible scheduling", description: "We work around your rotation"}, {method: "Before, during, or after deployment", description: "Support at every stage"}]

    3. **Family Members** (id: "families", shortName: "Families")
       - relevantPillarIds: ["wellbeing", "work-away", "community"]
       - accessPoints: [{method: "Events and programmes", description: "Join local gatherings and workshops"}, {method: "Direct contact", description: "Phone or email us any time"}, {method: "Local resources", description: "We'll connect you with support near you"}]

    4. **Community Members** (id: "community", shortName: "Community")
       - relevantPillarIds: ["wellbeing", "community"]
       - accessPoints: [{method: "Events and programmes", description: "See what's happening near you"}, {method: "Direct contact", description: "Phone, email, or drop in"}, {method: "Volunteer initiatives", description: "Get involved and give back"}, {method: "Partner organisations", description: "Through our network of local partners"}]

    Include audience-matched testimonials from CLAUDE.md sample testimonials.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- no type errors. Verify all four pillars have correct slugs matching the URL spec. Verify `getServiceBySlug("supporting-your-wellbeing")` returns the correct pillar. Verify all exports are named correctly.
  </verify>
  <done>
    Four ServicePillar objects with plain language names, four AudienceSegment objects with access points, all TypeScript types exported, `getServiceBySlug` helper working, zero type errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build PillarCard, WhatHappensWhenYouCall components, and Services overview page</name>
  <files>
    src/components/services/PillarCard.tsx
    src/components/services/WhatHappensWhenYouCall.tsx
    src/app/services/page.tsx
  </files>
  <action>
    **src/components/services/PillarCard.tsx** -- Create a reusable card component for displaying a service pillar on the overview page.

    Props: `pillar: ServicePillar`

    Structure:
    - Outer wrapper: `<Link href={/services/${pillar.slug}}>` wrapping a card div
    - Card has group hover behavior (Tailwind `group` class)
    - Top gold accent line: 2px height, `bg-accent-500`, transforms from `scaleX(0)` to `scaleX(1)` on group-hover, origin-left, 400ms transition with premium easing
    - Lucide icon rendered dynamically using the `pillar.icon` string (import Heart, Wheat, Anchor, Users from lucide-react; map icon name to component)
    - Icon in accent-500 color, 32x32
    - Pillar name as h3 in font-heading (Cormorant Garamond), text-2xl/font-semibold
    - Tagline as body text in text-body color
    - "Find out more" link text with right arrow, in accent-600 color
    - Card lifts 4px on hover with shadow increase (translateY(-4px), shadow-lg)
    - Background: white (or stone-50), rounded-lg, p-8
    - Respect prefers-reduced-motion: use Tailwind `motion-safe:` prefix for transforms

    **src/components/services/WhatHappensWhenYouCall.tsx** -- Create the reassurance section component (reused on every pillar detail page in Plan 02 and optionally on Services overview).

    Props: none (content is fixed)

    Structure:
    - Section with stone-100 or stone-200 background, generous padding (py-16 px-6)
    - Overline text: "WHAT HAPPENS WHEN YOU GET IN TOUCH" in uppercase, text-xs, font-bold, tracking-widest, text-accent-600
    - Heading: "It's simpler than you think" in font-heading, text-3xl
    - Four steps displayed as a numbered sequence (1-2-3-4 layout):
      1. "You call or fill in our form"
      2. "We'll have a friendly conversation to understand how we can help"
      3. "We'll suggest the right support for you"
      4. "You're in control -- no pressure, completely confidential"
    - Each step: a circle with the number (border-2 border-accent-500, w-10 h-10, flex items-center justify-center, rounded-full, font-heading font-semibold text-accent-600), then the step text
    - Steps connected by a subtle vertical line (border-l-2 border-stone-300) on mobile, or horizontal dots/line on desktop
    - Layout: vertical stack on mobile, horizontal row on desktop (md:flex-row)
    - Closing CTA: "Get in Touch" button linking to /contact
    - Use SectionWrapper if available from Phase 2 layout components

    **src/app/services/page.tsx** -- Create the Services overview page.

    This is a React Server Component (no "use client" directive).

    Structure:
    1. **Hero section** (stone-50 background):
       - Overline: "OUR SERVICES" in uppercase tracking-widest text-accent-600
       - Heading: "How We Can Help" in font-heading, text-display or text-5xl
       - Subtitle: "Whether you're looking for someone to talk to, practical support for your farm, connection while working away, or ways to strengthen your community -- we're here." in text-lg, text-body, max-w-2xl mx-auto
       - Centered text layout

    2. **Pillar cards grid** (stone-100 background):
       - Import servicePillars from data/services
       - 2x2 grid on desktop (md:grid-cols-2), single column on mobile
       - gap-8, max-w-5xl mx-auto
       - Map over servicePillars, render PillarCard for each
       - Add SectionWrapper with alternating background if available

    3. **WhatHappensWhenYouCall section**:
       - Render the WhatHappensWhenYouCall component
       - This gives users reassurance before they click into individual pillars

    4. **Bottom CTA section** (primary-900 dark background):
       - "Not sure where to start?" heading in text-on-dark
       - "That's completely fine. Get in touch and we'll help you find the right support." body text
       - "Get in Touch" button (accent-500 background, primary-950 text, hover effect)
       - Link to /contact

    Export generateMetadata:
    ```typescript
    export const metadata = {
      title: "Our Services | St Katharine Rural Connect",
      description: "Supporting your wellbeing, farm life, those who work away, and our community. Find out how St Katharine Rural Connect can help.",
    };
    ```
  </action>
  <verify>
    Run `npm run dev` and navigate to `/services`. Verify: (1) page renders without errors, (2) all four pillar cards display with correct names and icons, (3) each card links to `/services/{correct-slug}`, (4) gold accent line animates on card hover, (5) cards lift on hover, (6) WhatHappensWhenYouCall section displays all 4 steps, (7) page is responsive (single column mobile, 2x2 desktop), (8) "Get in Touch" CTA links to /contact. Run `npx tsc --noEmit` for type safety.
  </verify>
  <done>
    Services overview page at /services displays all four pillar cards with plain language names, hover effects with gold accent lines, WhatHappensWhenYouCall reassurance section, and dark CTA section. All cards link to correct pillar detail slugs. Page is responsive and type-safe.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `/services` page renders all four pillar cards with correct plain language names
- Pillar cards have working hover effects (gold line + lift)
- Each card links to `/services/supporting-your-wellbeing`, `/services/supporting-farm-life`, `/services/supporting-those-who-work-away`, `/services/strengthening-our-community`
- WhatHappensWhenYouCall component renders 4 numbered steps
- Data files export correct types and helper functions
- Page is responsive (mobile single column, desktop 2x2 grid)
</verification>

<success_criteria>
SERV-01 satisfied: Services overview page displays all four pillars with links to detail pages.
SERV-06 partially satisfied: User-centered plain language naming used throughout data and overview page.
SERV-07 partially satisfied: WhatHappensWhenYouCall component built and rendered on overview page (will also appear on detail pages in Plan 02).
Data foundation ready for Plan 02 (pillar details) and Plan 03 (Who We Serve).
</success_criteria>

<output>
After completion, create `.planning/phases/04-services-audience/04-01-SUMMARY.md`
</output>
